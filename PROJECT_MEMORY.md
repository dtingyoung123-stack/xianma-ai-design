# 项目记忆

本文档记录先马AI内容与视觉平台的重要功能、需求时间、确认结论和长期规则。后续询问“什么时候做的”“当时怎么定的”“需要遵守什么规则”时，以本文件、对应 PRD、`UI_SPEC.md` 和 Git 记录共同为准。

## 维护规则

- 每次完成新功能、需求调整、公共组件变更或重要问题修复后，更新本文件。
- 每条记录必须包含：日期、功能或需求、状态、确认范围、关键规则、关联文件。
- 需求发生变化时保留原结论，并追加新的变更日期，不直接抹掉历史。
- 日期优先记录实际需求确认或开发日期；与 Git 入库日期不一致时分别标明。
- Git 同步前检查本文件是否已覆盖本轮完成内容。

## 长期开发规则

1. 新功能先理解需求并给出具体方案，用户确认后再修改代码。
2. 开发前检查项目架构、UI 规范、现有框架和公共组件。
3. 优先复用现有组件；确需新增公共组件时，先说明职责、位置和复用范围并获得确认。
4. 只处理当前确认的问题，不修改、重构或格式化无关代码。
5. 前端原型需要提供完整交互，不在页面显示“API 未接入”等技术提示。
6. 公共能力在各业务页面保持统一样式和交互，页面只处理自己的业务状态。
7. 修改后执行相应的 ESLint、构建、页面交互、控制台、溢出和最终差异检查。
8. 用户要求同步 GitHub 时，先检查提交范围、敏感文件、未跟踪文件和远程分支，再提交推送。
9. 每次需求完成后同步更新 `PROJECT_MEMORY.md`；涉及产品定义时更新 `PRODUCT_REQUIREMENTS.md`，涉及 UI 或公共组件时更新 `UI_SPEC.md`。

## 当前技术栈

- 应用框架：Next.js 16，使用 App Router，页面和接口位于 `src/app/`。
- UI 运行时：React 19、React DOM 19。
- 开发语言：JavaScript 与 JSX；当前未启用 TypeScript。
- 样式方案：Tailwind CSS 4、PostCSS、CSS Variables；运行时设计 Token 统一维护在 `src/app/globals.css`。
- 组件体系：shadcn 4（`base-nova` 风格）与 Base UI React；基础组件位于 `src/components/ui/`。
- 公共业务组件：工作台通用能力位于 `src/components/workbench/`，业务页面优先复用现有组件。
- 图标体系：Lucide React。
- 样式工具：`clsx`、`tailwind-merge`、`class-variance-authority`、`tw-animate-css`。
- 代码检查：ESLint 9 与 Next.js Core Web Vitals 规则。
- 构建方式：Next.js 默认构建流程与 Turbopack。
- 版本管理与部署：GitHub `main` 分支连接 Vercel 自动部署，正式域名为 `https://ai.iiting.fun`。
- 当前产品形态：以前端交互原型和工作台为主，尚未接入独立后端框架、正式数据库及完整业务 API。
- 版本依据：依赖及准确版本以 `package.json` 和锁文件为准；架构边界以 `ARCHITECTURE.md` 为准。

## 功能时间线

### 2026-07-30：项目基础版本

- 状态：已入库。
- 完成统一导航、工作台框架、AI 买家秀、AI 商品套图、素材库、图片队列和 UI 规范页的基础原型。
- 关键规则：工作台页面复用 `src/components/workbench/`；运行时 UI 以 `src/app/globals.css` token 为准。
- Git：`c61944a Initial commit`。
- 关联文件：`ARCHITECTURE.md`、`UI_SPEC.md`、`src/components/workbench/Workbench.jsx`。

### 2026-07-31：AI 买家秀区域编辑

- 状态：前端交互原型已完成；整理入库时间为 2026-08-01。
- 功能：商品图片支持区域编辑、画笔遮罩、擦除、撤销、重做、清空、缩放、拖动和保存状态。
- 关键规则：区域数据跟随对应图片保存；关闭未保存不覆盖原数据；替换或删除图片时同步处理区域数据。
- 关联文件：`src/components/workbench/RegionMaskEditor.jsx`、`src/components/workbench/ImageQueueModule.jsx`、`src/app/ai-hub/BuyerShowPage.jsx`。

### 2026-07-31：区域编辑自定义文本标注

- 状态：公共前端组件能力已完成；整理入库时间为 2026-08-01。
- 功能：区域编辑器支持创建、编辑、拖动和删除自定义文本标注。
- 关键规则：不自动编号；按住圆点或文字框均可移动；单击文字框才修改内容；坐标基于图片保存，缩放后不偏移。
- 复用范围：属于公共区域编辑能力，不只服务 AI 买家秀。
- 关联文件：`src/components/workbench/RegionMaskEditor.jsx`、`8.1需求说明.md`。

### 2026-07-31：一键改字页面交互优化

- 状态：前端交互原型已完成；整理入库时间为 2026-08-01。
- 功能：素材选择、图片上传、全部文字识别、文字列表与图片选区联动、逐项修改、手动补充区域、拖动调整、撤销重做、模型与参数设置。
- 关键规则：不再要求用户单独填写“原文字”；识别不到文字时允许手动框选；页面复用公共图片、模型、参数和历史记录组件。
- 关联文件：`src/app/image-tools/text-edit/TextEditWorkbench.jsx`、`src/data/demo/text-edit.js`、`8.1需求说明.md`。

### 2026-07-31：子页面统一历史记录入口

- 状态：前端交互原型已完成；整理入库时间为 2026-08-01。
- 功能：子页面右上角提供统一历史记录入口，并携带来源页面和当前生成参数打开历史记录页面。
- 关键规则：入口位置和交互在工作台子页面保持一致；页面参数通过查询参数传递。
- 关联文件：`src/components/workbench/Workbench.jsx`、`src/app/history/HistoryClient.jsx`、`src/data/demo/history.js`。

### 2026-08-01：公共图片预览与吸色

- 状态：公共前端组件已完成。
- 功能：统一图片预览弹窗支持缩放、拖动、翻转、切换、下载和吸色；吸色后显示色块与 HEX，并自动复制，支持再次复制和删除。
- 关键规则：吸管只出现在公共图片预览工具栏；缩略图和图片队列标题不重复出现；不显示“产品主体”“参考色”等业务标签；不自动新增、修改或删除提示词。
- 当前覆盖：AI 买家秀、AI 商品套图、一键改字，以及后续复用 `ImageQueueModule` 的页面。
- 关联文件：`src/components/workbench/ImagePreviewModal.jsx`、`src/components/workbench/ColorConstraintPicker.jsx`、`src/components/workbench/ImageQueueModule.jsx`、`8.1需求说明.md`。

### 2026-08-01：UI 规范导出文件恢复

- 状态：已恢复并验证。
- 问题：`docs/前端UI规范与需求说明-V1.md` 未进入前两次 Git 提交，文件从本地消失后导致 `/api/ui-spec` 返回 500。
- 处理：从原始项目压缩包恢复文件，导出接口恢复为 200，并将文档加入 Git 跟踪。
- 关键规则：导出源文件必须纳入 Git；同步前检查 `docs` 和所有未跟踪项目文档。
- 关联文件：`docs/前端UI规范与需求说明-V1.md`、`src/app/api/ui-spec/route.js`、`src/app/ui-guide/page.js`。

## 当前需求文档

- `8.1需求说明.md`：本轮四项功能的开发 PRD。
- `PRODUCT_REQUIREMENTS.md`：产品需求池和后续规划。
- `UI_SPEC.md`：运行中的 UI 规则和公共组件清单。
- `docs/前端UI规范与需求说明-V1.md`：UI 规范页的可下载开发说明。
- `docs/素材库提示词库权限管理PRD_260810.md`：素材库、提示词库、权限管理与跨平台数据共享的一期 PRD。
- `AGENTS.md`：项目开发和协作执行规则。

### 2026-08-01：积分申请与系统管理员审批

- 状态：前端完整交互原型已实现；真实账号、权限、审批事务和积分入账 API 待后端接入。
- 确认范围：用户积分中心、积分申请、申请记录、积分流水、系统管理员审批；默认申请 1000，单次上限 5000。
- 权限规则：首期仅系统管理员审批；组织权限体系完成后再扩展部门或业务线审批配置。
- 关键规则：用户身份字段自动带出；同一用户不可同时存在多笔待审批申请；驳回必须填写原因；审批通过同步更新余额、申请状态和流水；已处理申请不可重复审批。
- 原型数据边界：演示数据位于 `src/data/demo/points.js`，页面通过 `src/lib/points-store.js` 读写浏览器本地状态，后续由真实 API 适配层替换。
- 入口规则：顶栏积分胶囊进入积分中心；首页个人信息卡在“可用积分”统计项下提供“申请积分”文字入口，同样跳转积分中心，不额外增加首页弹窗或状态逻辑。
- 关联文件：`src/app/points/page.js`、`src/app/admin/points-requests/page.js`、`src/app/home/page.js`、`src/components/Topbar.jsx`、`src/config/navigation.js`、`PRODUCT_REQUIREMENTS.md`。

### 2026-08-03：积分审批待审批演示数据

- 状态：前端演示数据已补充。
- 调整内容：新增两条不同申请人、部门和申请金额的待审批数据，用于验证审批列表、详情、通过和驳回交互。
- 数据边界：申请人和账号均为虚构测试信息；原型本地存储版本升级为 `v3`，首次读取时恢复新版演示数据。
- 关联文件：`src/data/demo/points.js`、`src/lib/points-store.js`。

### 2026-08-03：积分申请审批 PRD 导出

- 状态：前端导出入口与 Markdown 下载接口已实现。
- 功能：积分中心标题区右上角提供“导出 PRD”，点击后直接下载 `积分申请与审批模块PRD.md`。
- 统一规则：后续 PRD 导出复用 `PageShell` 的 `actions` 操作位和 UI 规范页的下载按钮样式；源文件统一放在 `docs/` 并纳入 Git。
- 异常边界：源文件读取失败时返回明确错误状态，不生成空文件。
- 关联文件：`src/app/points/page.js`、`src/components/PageShell.jsx`、`src/app/api/points-prd/route.js`、`docs/积分申请与审批模块PRD.md`、`UI_SPEC.md`。

### 2026-08-03：顶栏个人信息面板

- 状态：前端交互原型已实现。
- 功能：点击右上角头像展示用户身份、可用积分、今日使用、本年使用和待同步事件，并提供积分中心、修改密码、刷新账号信息和退出登录入口。
- 数据边界：身份与待同步事件使用演示用户数据；积分及使用量从浏览器本地积分状态计算；账号类入口仅保留前端产品形态，不代表真实账号接口已接入。
- 关联文件：`src/components/Topbar.jsx`、`src/data/demo/points.js`、`UI_SPEC.md`。

### 2026-08-03：业务权限、模型管理与系统设置原型

- 状态：前端交互原型已实现；聚合账号接口、服务端鉴权和持久化待正式接入。
- 确认范围：先马AI内容与视觉平台不重复建设账号和组织管理；聚合管理平台继续提供用户、组织层级、所属部门、登录权限和账号状态。
- 权限规则：平台固定普通用户、部门管理员、系统管理员三个角色；部门管理员配置授权组织并自动包含下级组织；普通用户仅本人数据，系统管理员全公司数据。
- 页面结构：新增业务权限管理页面；将现网“大模型管控”归类为模型管理并保留全部模型配置项；系统设置保留公告、停站、默认大语言模型和系统提示词。
- 后续复用：素材、商品和场景上传时的可见组织不得超过上传人的授权组织范围；历史记录与数据看板统一按业务数据权限过滤。
- 演示数据边界：管理页演示账号、组织、模型和系统提示词位于 `src/data/demo/admin.js`，不代表聚合平台真实数据已接入。
- 关联文件：`src/app/admin/permission/page.js`、`src/app/admin/models/page.js`、`src/app/admin/settings/page.js`、`src/data/demo/admin.js`、`src/config/navigation.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`。

### 2026-08-04：权限管理改为钉钉主管自动授权

- 状态：已确认并更新前端交互原型；真实钉钉/聚合接口、服务端鉴权和持久化仍待接入。
- 规则变更：本条覆盖 2026-08-03 的逐人角色配置规则。聚合管理平台同步钉钉账号、组织和主管标识；钉钉主管自动成为部门管理员并管理所属部门及全部下级部门。
- 平台边界：先马AI内容与视觉平台不维护组织架构，不支持自定义角色；仅维护系统管理员、普通员工特殊授权和主管跨部门范围。
- 页面结构：权限人员主列表保持平铺并展示全部同步人员；提供分页、搜索和部门/角色筛选，普通员工从列表行内发起授权；组织树仅在授权弹窗中用于选择管理数据范围。
- 交互规则：支持同步反馈、最近同步时间、添加特殊授权、编辑角色与范围、取消特殊授权；主管基础部门不可移除，取消主管特殊授权后仍保留自动权限，至少保留一名有效系统管理员。
- 数据口径：普通用户的个人历史和经营数据限本人，组织可见的素材、商品和场景按资源可见范围展示；管理数据范围与资源可见范围分开判断。
- 服务端边界：授权根组织按最新组织树动态展开；最后一名系统管理员保护需覆盖并发降权和上游停用；正式接入必须补齐越权拒绝与权限变更审计。
- 关联文件：`src/app/admin/permission/page.js`、`src/data/demo/admin.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`。

### 2026-08-04：平台名称调整

- 状态：已确认并同步当前原型与项目文档。
- 命名规则：平台对外展示名称由“先马 AI 设计平台”调整为“先马AI内容与视觉平台”。
- 技术边界：项目目录、包名、路由和其他技术标识保持不变。
- 关联文件：`src/components/Topbar.jsx`、`src/app/layout.js`、`src/app/ui-guide/page.js`、`README.md`、`PRODUCT_REQUIREMENTS.md`、`PROJECT_MEMORY.md`、`UI_SPEC.md`、`docs/`。

### 2026-08-04：平台角色改为可叠加多选

- 状态：已确认并更新前端交互原型；角色管理页面和真实角色接口后续建设。
- 规则变更：本条补充并覆盖同日的单角色分配交互。所有有效账号固定拥有普通用户基础权限；钉钉主管自动叠加部门管理员角色；平台手动角色使用下拉框多选，功能和数据范围按并集合并。
- 后台边界：只有系统管理员可以查看和访问数据智能、积分审批、权限管理、模型管理与系统设置；部门管理员只提供部门数据范围，不获得后台管理页面权限。
- 扩展边界：首期角色选项来自内置演示配置，不新增角色管理页面；后续角色管理作为统一角色来源，权限配置页直接读取新增角色。
- 交互规则：列表角色以多个标签展示，行操作统一为“配置”；主管自动角色在下拉框中选中且不可取消，清除手动配置后仍保留普通用户和主管自动角色。
- 关联文件：`src/app/admin/permission/page.js`、`src/data/demo/admin.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`。

### 2026-08-04：权限列表简化与数据范围提示

- 状态：已确认并更新前端交互原型。
- 列表规则：删除“权限来源”列，保留主管标识、平台角色和管理数据范围三个直接判断字段；详细来源由后续审计记录承载。
- 弹窗规则：系统管理员固定为全公司范围，不显示组织选择；取消系统管理员且仍有部门管理员角色时，组织树重新出现。
- 关联文件：`src/app/admin/permission/page.js`、`PRODUCT_REQUIREMENTS.md`、`PROJECT_MEMORY.md`、`UI_SPEC.md`。

### 2026-08-04：组织树更新与 BP 特殊授权

- 状态：已确认并更新前端交互原型；正式组织数据仍待聚合管理平台接口接入。
- 事实来源：用户指定《公司记忆》2026-06-30 组织架构作为本轮原型依据；原型录入 10 个职能中心、4 个事业群和资料中明确记录的下级部门。
- 数据边界：组织人数、备注和“直属/未归入子部门”不进入权限树；资料未记录下级层级的组织保持一级展示，不推测补齐。
- 特殊授权：非主管 BP 可由系统管理员手动叠加部门管理员角色并选择跨部门数据范围，不因此获得后台管理页面权限。
- 交互规则：公司根组织只读，职能中心和事业群均作为同级一级组织直接展示，不新增中间分组节点；组织树支持搜索、展开收起、父组织包含下级、部分下级半选和搜索空状态，默认展开所属及已授权分支。
- 正式接入：公司记忆仅用于原型演示；生产运行时以聚合管理平台同步的稳定组织 ID、名称和最新层级为唯一来源。
- 关联文件：`src/data/demo/admin.js`、`src/app/admin/permission/page.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`。

### 2026-08-04：事业群下级组织补充

- 状态：已根据用户补充的两张钉钉组织树截图修正前端原型。
- 新增层级：创新事业群下补充碧落天事业部、天地会事业部、多宝阁事业部、无极殿事业部；一马事业群下补充真武殿事业部、九霄殿事业部、一马事业群~商品部、护具事业部、抖音事业部；众创事业群下补充业务中心、众创事业群~商品部、花果山、承晖阁；喜佑事业群下补充朗臻天业务中心、兜率天中后台。
- 数据边界：截图中的人数和个人主管、BP 节点不进入组织权限树，不根据人数差额推测其他节点。
- 关联文件：`src/data/demo/admin.js`、`PRODUCT_REQUIREMENTS.md`、`PROJECT_MEMORY.md`。

### 2026-08-04：组织树取消职能与事业群分组

- 状态：已确认并更新前端交互原型。
- 规则变更：职能中心和事业群均作为公司根组织下的同级一级组织，页面不显示“职能中心”或“事业群”中间分组标题。
- 交互保持：组织搜索、展开收起、父组织包含下级、部分下级半选和默认展开授权分支的规则不变。
- 关联文件：`src/app/admin/permission/page.js`、`src/data/demo/admin.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-04：正式组织树同步到最末级

- 状态：已确认并写入权限底座 PRD；当前原型不继续补充演示组织层级。
- 正式规则：生产环境通过聚合管理平台获取完整钉钉组织架构，从公司根组织递归同步到所有最末级组织节点，不限制为固定二级或三级。
- 来源边界：技术接口统一对接聚合管理平台，但组织架构的业务事实源必须是钉钉；聚合管理平台负责向先马AI内容与视觉平台同步钉钉账号、组织层级、所属部门和主管标识。
- 展示与权限：权限树按真实父子关系展示任意深度组织；选择任意父组织时自动包含其所有层级的后代组织。
- 原型边界：《公司记忆》和当前演示数据只用于原型展示，不代表正式组织树的深度上限。
- 关联文件：`PRODUCT_REQUIREMENTS.md`、`PROJECT_MEMORY.md`。

### 2026-08-04：素材库团体共享与轻量审核

- 状态：前端交互原型已实现；真实素材服务、组织权限、提交审核和持久化接口待接入。
- 页面范围：素材库保留个人与公共素材能力，新增团体素材和页内待审核视图，不新增独立审批页面。
- 低门槛提交：普通用户从个人素材一键提交到本人所属钉钉部门，不需要选择组织；审核中仍保留在个人素材，可取消提交，驳回后显示原因并支持重新提交。
- 权限规则：部门管理员审核并管理自身数据范围内的团体素材，通过时可调整可见组织；系统管理员管理全公司团体素材和公共素材；选择父组织自动包含所有层级下级组织。
- 审批入口：部门管理员和系统管理员在素材库顶部始终可见“待我审批（数量）”，点击直接进入待审核列表；系统管理员查看全公司待审核内容，部门管理员只查看管理范围内内容，普通用户不显示该入口。
- 跨模块复用：买家秀、商品套图和一键改字的共享素材选择弹窗新增团体素材库入口，并沿用各工具原有选择数量和回填逻辑。
- 原型数据边界：素材、人员、组织范围和审核状态均为隔离演示数据，不代表真实接口已接入。
- 关联文件：`src/app/materials/page.js`、`src/app/materials/MaterialsClient.jsx`、`src/data/demo/materials.js`、`src/components/workbench/AssetPickerModal.jsx`、`src/data/demo/asset-picker.js`、`src/data/demo/text-edit.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-04：一期审批权限底座

- 状态：权限管理前端交互原型已更新；主管默认审批覆盖范围和积分审批归属待 2026-08-05 与业务确认。
- 内置身份：普通用户、部门管理员、审批员、系统管理员；一期不新增角色管理页面。
- 自动规则：钉钉主管自动获得部门管理员和所属及下级组织的默认审批能力；系统管理员固定拥有全部页面、全部数据和全部审批。
- 特殊规则：非主管需要审批时，由系统管理员选择审批员角色、审批事项和负责组织；当前演示审批事项为素材入库审批、积分申请审批。
- 权限边界：页面权限、操作权限、组织数据范围和单条资源可见范围独立判断；资源可见范围仍在素材、商品、场景发布或审核时配置。
- 扩展方向：后续角色管理定义页面和操作权限，权限管理继续分配人员角色与组织范围；底层预留角色、权限点、审批事项、组织范围和资源可见范围关系。
- 原型边界：人员、角色、审批事项和组织范围均为隔离演示状态，真实钉钉同步、服务端鉴权、持久化与审计尚未接入。
- 关联文件：`src/data/demo/admin.js`、`src/app/admin/permission/page.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-05：主管、审批员与部门管理员权限拆分

- 状态：已确认并更新前端交互原型；本条覆盖 2026-08-04“主管自动成为部门管理员”的旧规则。
- 主管规则：钉钉主管是同步标识，不是平台角色；主管自动获得所属及下级组织数据范围，一期默认叠加审批员。系统管理员可取消个别主管的审批员，取消后仍保留主管数据范围。
- 角色边界：审批员负责所选事项与组织范围内的审批，不获得团体资源编辑、删除权；部门管理员负责指定组织团体资源的编辑、删除，不自动获得审批；同一人员需要两类能力时叠加两个角色。
- BP 规则：BP 只负责查看数据和审批时配置“审批员 + 指定组织范围”；只有同时负责团体资源维护时才增加部门管理员。
- 组织边界：人员可从完整钉钉组织架构同步，但平台可授权范围只展示众创事业群及其任意层级下级组织，不允许授权其他顶层一级部门。
- 素材联动：素材库“待我审批”入口改为判断审批员，团体素材编辑和删除改为判断部门管理员；系统管理员继续拥有全部能力。
- 页面补充：权限管理页新增角色权限表，按系统管理员、部门管理员、审批员、普通用户顺序展示数据、审批、资源操作、后台页面与获得方式。
- 原型边界：当前仍为隔离演示状态，真实钉钉同步、服务端鉴权、审批待办失效、持久化与审计待正式接入。
- 关联文件：`src/app/admin/permission/page.js`、`src/app/materials/MaterialsClient.jsx`、`src/data/demo/admin.js`、`src/data/demo/materials.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-05：权限说明收起与主管默认角色调整

- 状态：已确认并更新前端交互原型；本条覆盖同日“主管只默认叠加审批员”的旧规则。
- 主管规则：钉钉主管一期默认叠加部门管理员和审批员，两项可分别取消；取消任一默认角色后仍保留所属及下级组织的数据查看范围。清除手动配置后恢复两项主管默认角色。
- 系统管理员口径：系统管理员固定拥有全部数据和全部审批，不受众创事业群授权范围限制；众创事业群限制仅适用于其他人员的组织授权。
- 页面交互：角色权限表不再默认直出，改为“管理权限人员”标题旁帮助图标触发的角色权限说明弹窗；平台角色和审批事项多选支持点击外部自动收起，内部连续勾选时保持展开。
- 角色边界：部门管理员继续只增加团体资源编辑、删除权，审批员继续只增加所选事项审批权，两者仍需独立判断。
- 关联文件：`src/app/admin/permission/page.js`、`src/data/demo/admin.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-05：提示词库 0-1 交互原型

- 状态：前端交互原型已实现；真实提示词服务、外部同步、组织权限、审批事务、跨工具回填和埋点接口待接入。
- 页面定位：新增独立 `/prompts` 页面，按团队提示词、我的提示词、灵感广场三个内容域管理；提示词从素材库文件类型中移除，素材库继续管理图片、视频和音频。
- 外部数据：现网外部提示词整体归入灵感广场，只作为灵感来源，不自动成为公司已验证提示词；系统管理员保留来源启停、立即更新、新增来源和屏蔽标签入口。
- 个人与团队闭环：支持新建、编辑、复制、收藏外部提示词、提交团队、取消提交、审核通过、驳回和重新提交；普通用户提交时默认进入本人所属钉钉部门，不自行选择可见组织。
- 权限规则：新增提示词入库审批事项；审批员处理所选组织范围内的提示词申请并在通过时设置可见组织，部门管理员管理授权组织团队提示词，系统管理员管理全部数据和外部来源。
- 使用联动：提示词详情可选择目标工具、编辑本次使用内容，并通过“复制并打开工具”进入对应工具；自动回填待各工具统一接入提示词草稿协议后实现。
- 埋点边界：提示词库后续需要记录访问、搜索、筛选、详情、复制、收藏、创建、提交、审批和使用等行为；正式事件名与成功指标在最终 PRD 中统一定义。
- 原型数据边界：提示词、来源、同步时间、使用次数、人员和组织均为隔离演示数据，不代表生产接口已接入。
- 关联文件：`src/app/prompts/page.js`、`src/app/prompts/PromptsClient.jsx`、`src/data/demo/prompts.js`、`src/config/navigation.js`、`src/data/demo/admin.js`、`src/app/materials/MaterialsClient.jsx`、`src/data/demo/materials.js`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-06：提示词库改为文本资产列表

- 状态：已确认并更新前端交互原型；本条修正上一版将提示词做成图片素材卡片的展示方式。
- 展示规则：提示词不设置封面图，团队提示词、我的提示词和灵感广场统一使用单列紧凑文本列表，直接展示名称、正文摘要、业务场景、适用工具、标签、来源和状态。
- 操作规则：复制为最高频主操作；团队内容提供复制和详情，个人内容提供复制、编辑、提交团队、取消提交和删除，外部灵感提供复制和收藏；审批员在待审核列表执行通过或驳回。仅在详情中且提示词已绑定平台工具时提供“去使用”。
- 交互边界：新建、编辑、提交、审批、来源管理、搜索和筛选继续保留；详情与审批弹窗不展示图片。所有操作均为前端隔离演示状态，不代表真实接口已接入。
- 关联文件：`src/app/prompts/PromptsClient.jsx`、`src/data/demo/prompts.js`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-06：提示词用途分类简化

- 状态：已确认并更新前端交互原型。
- 分类规则：业务场景统一更名为“用途分类”，新增“通用”；一期分类固定为通用、商品主图、详情页、买家秀、场景图、营销海报和短视频。
- 交互规则：删除适用工具筛选、列表与详情工具标签、新建/编辑工具字段和选择工具打开页面的交互；提示词以复制使用为主，降低创建与检索门槛。
- 扩展边界：演示数据中的工具关联字段暂作兼容保留，正式数据模型可将工具关联作为可选字段；待后续具备明确的工具回填协议后再评估开放，不要求一期用户维护。
- 关联文件：`src/app/prompts/PromptsClient.jsx`、`src/data/demo/prompts.js`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-06：素材库 UI 对齐提示词库

- 状态：已确认并更新前端交互原型。
- 统一范围：素材库的一级范围 Tab、团体二级切换、搜索与类型筛选、结果数量、操作按钮、卡片阴影、空状态及弹窗视觉统一采用提示词库规范。
- 保留边界：素材继续使用固定 `4:3` 预览卡片网格，不改为提示词的文本列表；上传、分页、提交团队、审批、组织可见范围和角色权限逻辑不变。
- 实现边界：本次仅调整素材库页面局部结构与样式，不新增共享组件，不修改演示数据和其他页面。
- 关联文件：`src/app/materials/MaterialsClient.jsx`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-07：提示词取消适用模型限制

- 状态：已确认并更新前端交互原型。
- 使用规则：同类型模型默认均可使用同一提示词，模型差异只影响生成效果，不作为提示词使用限制。
- 交互规则：提示词详情不再展示“适用模型”，新建和编辑不要求用户选择模型，页面不提供模型筛选。
- 扩展边界：后续如需沉淀模型效果，可增加“已验证模型”记录，但仅表示团队测试依据，不限制其他同类型模型使用；现有演示数据中的历史模型字段暂保留兼容但前端不读取。
- 关联文件：`src/app/prompts/PromptsClient.jsx`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-10：资源管理与入库审批合并

- 状态：已确认并更新前端交互原型；本条覆盖 2026-08-05“部门管理员不自动获得审批”的旧规则。
- 权限规则：部门管理员自动拥有授权组织内的团体资源编辑、删除，以及素材和提示词入库审批；审批员继续用于给非管理员授权资源审批，或按事项配置积分等独立审批。
- 主管规则：一期仍按既定方案默认叠加部门管理员和审批员，不删除已有角色交互；资源入库能力重复命中时按一份权限处理。
- 扩展边界：一期不开放普通员工跨部门只读组织范围；底层保留独立的数据范围判断，后续新增该能力不改变角色、资源可见范围及看板过滤结构。
- 素材流程：审核中的个人素材隐藏编辑和删除，必须先取消提交，恢复为普通个人素材后才能删除。
- 原型修正：素材上传仅接受并描述图片、视频和音频；权限人员表合并为六列并适配 1280px；提示词使用次数为 `0` 时显示 `0`。
- 关联文件：`src/app/admin/permission/page.js`、`src/app/materials/MaterialsClient.jsx`、`src/app/prompts/PromptsClient.jsx`、`src/data/demo/admin.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-10：积分审批保持系统管理员专属

- 状态：已确认并更新前端交互原型；与积分申请审批既有一期规则保持一致。
- 权限规则：积分申请审批一期仅系统管理员拥有；主管、部门管理员和审批员不默认获得，也不能在人员权限配置中手动勾选。
- 交互规则：非系统管理员的审批事项下拉保留“积分申请审批”作为权限边界说明，但固定未勾选、不可操作并标注“仅系统管理员”；系统管理员继续显示“全部审批”。
- 扩展边界：后续如需将积分审批下放给其他角色，必须作为独立需求重新确认，不由当前审批员角色默认继承。
- 关联文件：`src/app/admin/permission/page.js`、`src/data/demo/admin.js`、`PRODUCT_REQUIREMENTS.md`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-10：素材库、提示词库与权限管理一期 PRD

- 状态：PRD 已更新至 V1.1，业务规则已确认、待技术方案；当前原型仍为前端演示，真实服务端能力尚未据此完成。
- 文档范围：整合素材库、提示词库、权限管理三大模块，明确角色权限、组织与资源可见范围、审批流程、状态、数据对象、埋点、审计和验收标准。
- 跨平台规则：一期仅向其他 AI 平台提供受权限控制的数据查询、详情读取、素材获取、提示词获取和使用回传；二期预留外部新增、更新及提交审批，外部写入不得绕过个人归属、组织可见范围和团队审批。
- 评审确认：一期开放灵感广场数据调用并明确未验证标识；任一审批人处理后实时更新其他人的待办，旧页面再次审批提示已被处理；新增审批事项不自动授权；文件限制和数据保留沿用当前系统规则；来源同步策略由技术维护；满足现有权限规则的审批人允许审批本人提交的资源。
- PRD 文档规则：Markdown 为事实源，默认存放在项目 `docs/`；文件名统一为 `功能名称PRD_YYMMDD.md`，日期使用实际创建日期。后续修订优先更新原文件版本，不重复创建日期副本，除非用户明确要求快照。
- 关联文件：`docs/素材库提示词库权限管理PRD_260810.md`、`AGENTS.md`、`PRODUCT_REQUIREMENTS.md`、`PROJECT_MEMORY.md`。

### 2026-08-10：五个模块交付状态更新

- 状态：已按用户本轮确认更新前端原型状态标签和 UI 页面清单。
- 开发中：素材库、提示词库、权限管理。
- 已上线：AI 买家秀、AI 商品套图。
- 实现规则：使用 `PageShell` 的页面状态来自 `src/config/navigation.js`；`PageShell` 新增 `development` 和 `live` 显示映射。AI 买家秀、AI 商品套图使用独立 `WorkbenchShell`，页面头部同步传入“已上线”。
- 关联文件：`src/config/navigation.js`、`src/components/PageShell.jsx`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-10：权限管理增加 PRD 导出入口

- 状态：前端入口与 Markdown 下载接口已实现。
- 页面规则：权限管理页右上角单独展示“导出 PRD”，位于“同步钉钉”上方，不与业务功能按钮并排组合。
- 下载规则：点击后直接下载 `docs/素材库提示词库权限管理PRD_260810.md`；源文件缺失时返回明确错误，不生成空文件。
- 关联文件：`src/app/admin/permission/page.js`、`src/app/api/permission-prd/route.js`、`docs/素材库提示词库权限管理PRD_260810.md`、`PROJECT_MEMORY.md`。

### 2026-08-11：权限管理人员列表 PRD 字段契约补全

- 状态：PRD 已更新至 V1.2；未改动前端原型或服务端实现。
- 文档规则：将“钉钉身份”和“账号状态”明确为独立字段；列出人员、角色、组织数据范围、审批权限、筛选、分页、统计和配置弹窗的字段、来源、状态枚举、空值、可编辑边界与按钮结果。
- 账号状态：正式接口枚举统一为 `ACTIVE`、`DISABLED`，当前原型 `active`、`disabled` 仅为映射；停用账号保留在列表、不计入三个统计、不可配置且立即失去访问能力。
- 待确认：停用账号的既有平台授权是否保留、恢复账号后是否自动恢复，尚未形成业务与技术确认，不写入正式执行规则。
- 关联文件：`docs/素材库提示词库权限管理PRD_260810.md`、`PROJECT_MEMORY.md`、`C:\Users\Administrator\.codex\skills\xianma-ai-design-platform-prd\SKILL.md`、`C:\Users\Administrator\.codex\skills\xianma-ai-design-platform-prd\references\review-checklist.md`。

### 2026-08-11：生成参数支持自定义图片尺寸

- 状态：已确认并更新前端交互原型；当前仅为前端演示状态，实际生成服务的尺寸能力和模型限制待服务端对接时校验。
- 交互规则：AI 买家秀和一键改字复用 `WorkbenchParameterSelect` 的“图片尺寸”配置；除预设比例外新增“自定义”，选择后在同一弹层输入宽度和高度（像素）。默认值为 `800×800`，适配淘工厂主图等固定像素场景。
- 数据规则：自定义宽高必须均为正整数；未填写完整时不可提交生成。任务摘要和历史记录展示实际像素尺寸，例如 `800×800px`，而非“自定义”。
- 边界：AI 商品套图的主副图比例和 A+ 固定尺寸继续由其自身的平台规则控制，不接入本次公共尺寸输入。
- 关联文件：`src/components/workbench/WorkbenchControls.jsx`、`src/app/ai-hub/BuyerShowPage.jsx`、`src/app/image-tools/text-edit/TextEditWorkbench.jsx`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-11：本地原型 127.0.0.1 交互恢复

- 状态：已修复本地开发环境中页面能渲染但按钮无法交互的问题。
- 原因与规则：Next.js 开发服务默认拦截来自 `127.0.0.1` 的开发资源请求，导致客户端交互未挂载；开发配置显式允许 `127.0.0.1`，本地原型统一可通过 `http://127.0.0.1:3000` 访问。
- 关联文件：`next.config.mjs`、`PROJECT_MEMORY.md`。

### 2026-08-13：生成参数图片尺寸交互调整

- 状态：已按最新确认规则更新前端交互原型；实际生成服务的尺寸支持和输出一致性仍待服务端验证。
- 交互规则：AI 买家秀和一键改字的“图片尺寸”删除“自定义”比例项，宽度和高度输入区固定显示。选择预设比例后，用户可选填宽度，高度按所选比例自动计算且只读；宽度留空时由 AI 决定实际生成尺寸。
- 智能比例：选择“智能比例”时，宽度和高度均禁用，尺寸摘要继续显示“智能比例”。
- 模型边界：界面提示自定义像素仅 GPT 模型支持，且实际输出尺寸可能存在偏差；本次仅更新前端原型提示，不代表已完成模型能力校验或服务端限制。
- 关联文件：`src/lib/image-size.js`、`src/components/workbench/WorkbenchControls.jsx`、`src/app/ai-hub/BuyerShowPage.jsx`、`src/app/image-tools/text-edit/TextEditWorkbench.jsx`、`tests/image-size.test.mjs`、`PROJECT_MEMORY.md`。

### 2026-08-14：全站山茶红交互主题

- 状态：项目实施已确认，前端主题改造已完成并通过当前可执行验证；不表示领导已审批、已部署或已上线。
- 主题规则：全站品牌交互统一使用山茶红 `#D9353F`，Hover 使用 `#C22F38`，Active/Deep 使用 `#A92831`，Soft 使用 `rgba(217, 53, 63, 0.08)`，Focus 使用 `rgba(217, 53, 63, 0.14)`，Border 使用 `rgba(217, 53, 63, 0.15)`。
- 语义边界：按钮、导航、链接、选中态、表单焦点、进度和蒙版使用品牌 Token；Success、Warning、Danger、Info 继续使用独立语义色。蓝灰工作台背景、登录页、Logo、插图和其他视觉资产未调整。
- 实现边界：未新增共享组件，未调整路由、业务数据或交互流程；区域蒙版 Canvas 动态读取全局品牌 Token，UI 规范页直接展示正式全局主题。
- 验证结果：正式源码旧品牌蓝色静态扫描通过；25 个导航页面均返回 200；源码 ESLint 0 error（保留 5 个既有图片性能 warning）；图片尺寸测试 4/4 通过；`next build --webpack` 通过；重点页面在 1280px 与当前窄屏浏览器视口下无横向溢出或项目控制台错误。1024px 与 390px 的浏览器视口覆盖在当前工具版本未生效，需后续人工补验。
- 关联文件：`src/app/globals.css`、`src/app/ui-guide/page.js`、`src/components/workbench/Workbench.jsx`、`src/components/workbench/AssetPickerModal.jsx`、`src/components/workbench/RegionMaskEditor.jsx`、`UI_SPEC.md`、`docs/前端UI规范与需求说明-V1.md`、`PROJECT_MEMORY.md`。

### 2026-08-14：历史记录前端原型优化

- 状态：现有功能复刻与 UI 布局优化已完成，并通过当前可执行验证；仅代表前端原型更新，不表示服务端改造、部署或上线。
- 功能范围：保留“我的记录 / 所有记录”、记录总数、账号/日期/功能筛选、刷新、结果预览、收藏、复制提示词、删除、详情、整条加入素材库、继续编辑和分页；功能点覆盖专家模式、一键改字、主体替换、产品微调、批量美颜、批量改图、AI能力中枢、AI多角度、AI买家秀、AI视频流、AI商品套图。
- 权限边界：“所有记录”和账号筛选属于现网已有入口，本期予以保留；未新增角色、组织数据范围或服务端鉴权规则，真实数据接入前仍需由业务确认可见角色。
- 数据边界：原型仅使用 `src/data/demo/` 下的脱敏演示数据，页面组件未写入现网真实账号、提示词或业务记录。
- UI 规则：复用 `PageShell`、现有 Button、SafeImage 与全局山茶红 Token；卡片相较上一版宽度约缩小 33%，1280px 三列、1024px 双列、390px 单列。点击卡片主体或“查看详情”均打开弹窗，详情按元信息、提示词、原图、结果图分区展示；详情/删除弹窗通过 Portal 约束在视口内。未新增共享组件、路由或依赖。
- 验证结果：源码 ESLint 0 error（保留 5 条既有图片性能 warning）；`next build --webpack` 通过；浏览器确认 11 个功能点均有当前账号脱敏记录，1280px 卡片宽 321px、页面三列，390px 卡片宽 353px；桌面和窄屏详情均可见提示词、原图和结果图，页面无横向溢出或控制台错误。
- 关联文件：`src/app/history/HistoryClient.jsx`、`src/data/demo/history.js`、`PROJECT_MEMORY.md`。

### 2026-08-15：素材、提示词公共选择器及专家模式完善

- 状态：已按确认方案完成前端交互原型并通过当前可执行验证；真实资源接口、服务端鉴权、审批服务和生成任务服务尚未据此完成，不表示已部署或上线。
- 素材选择器：统一为个人素材、团体素材、公共素材、本地上传四个扁平来源，保留搜索、固定一级类目、标签、多选上限、本地上传、保存个人素材、加载/错误/无权限/空状态；固定类目为通用、医疗器械类、大健康类、宠物类、护具类、美妆类、进口保健品类、个护类、地毯类，新上传默认通用，历史缺失显示未分类，本期不提供类目管理和二级分类。
- 提示词选择器：统一为个人提示词、团队提示词、公共提示词、灵感广场四个扁平来源，保留搜索、用途筛选、正文预览、单选、清空及加载/错误/空状态；公共提示词标记公司已验证，灵感广场显式标记未经验证。
- 审批规则：素材与提示词均为个人到团队、团队到公共的逐级发布；公共发布仅系统管理员审批，申请期间原团队资源继续可用。素材库和提示词库均增加团队入库与公共发布待办、申请、取消、通过和驳回演示流程。
- 专家模式：以现网实际上传能力为准统一支持 0～14 张参考图，并保留纯文生图；支持素材选择、本地上传、排序、预览、替换、删除、区域编辑、提示词编辑/预览/模板/清空/AI 润色、5 个模型、1K/2K/4K、8 种比例、1～9 张输出、异步进度、请求 ID、结果下载、页内历史刷新/分页/复制/继续编辑。
- 共享组件：新增 `WorkbenchPickerDialog` 作为素材与提示词大型选择弹窗母版，统一 Portal、Esc、遮罩关闭、焦点循环、焦点返回、页面滚动锁定、固定标题/操作区和内部滚动；`WorkbenchShell` 增加可选内容类名，专家模式在 1100px 以下改为单栏，不影响其他工作台。
- 数据边界：新增及修改的演示状态均位于 `src/data/demo/`，未把现网账号、真实素材、真实提示词或任务数据写入页面组件。
- 验证结果：源码 ESLint 0 error（保留 5 条既有图片性能 warning）；`next build --webpack` 通过；浏览器验证 1280px 双栏、1024px/390px 单栏及三个页面无横向溢出，两个共享选择弹窗可覆盖侧栏并在 1024px/390px 保持固定头尾，Esc 关闭后焦点返回触发按钮；专家模式素材回填、公共提示词回填、纯文生图、异步完成、请求 ID 和下载入口均可用；干净页面控制台无错误。
- 关联文件：`src/components/workbench/WorkbenchPickerDialog.jsx`、`src/components/workbench/AssetPickerModal.jsx`、`src/components/workbench/PromptPickerModal.jsx`、`src/components/workbench/Workbench.jsx`、`src/app/materials/MaterialsClient.jsx`、`src/app/prompts/PromptsClient.jsx`、`src/app/image-tools/expert/ExpertWorkbench.jsx`、`src/data/demo/materials.js`、`src/data/demo/prompts.js`、`src/data/demo/expert.js`、`src/app/globals.css`、`docs/素材库提示词库权限管理PRD_260810.md`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-15：素材与提示词全状态及公司资产删除权限

- 状态：业务规则已确认并授权前端原型实施；真实审批服务、账号停用归档、服务端鉴权、审计和恢复能力待后端实现，不表示已部署或上线。
- 状态展示：个人范围统一展示可用、团队审核中、团队已驳回；团队范围统一展示团队可用、公共审核中、公共已驳回；公共范围仅展示公共可用，灵感广场不进入内部审批状态筛选。素材演示数据覆盖图片、视频、音频和各适用状态，提示词覆盖个人、团队、公共及审批状态。
- 提交提示：个人提交团队前明确提示审批通过后发布人不能自行删除、需联系团队负责人；团队申请公共前明确提示发布人及团队负责人不能自行删除、需联系系统管理员。素材与提示词共用同一业务文案和权限口径。
- 删除权限：审核中必须先取消申请再编辑或删除；团队审批通过后由团队负责人管理，公共审批通过后仅系统管理员管理。发布人身份本身不授予已共享公司资产的编辑、删除权。
- 离职处理：账号停用不得级联删除素材或提示词；团队和公共资产继续可用并保留发布人、审批与审计信息，个人范围内容进入后台归档。本期不新增离职交接页面。
- 交互边界：管理页展示完整状态和管理操作，工作台素材/提示词选择弹窗继续只展示当前可用内容；公共审核期间原团队内容继续可用但必须先取消申请才能编辑或删除。
- 全局验收补漏：无团队待办时“待我审批”不会把非系统管理员带入公共审批视图；公共申请仅发起人或系统管理员可取消；非系统管理员审批和管理团体资源时，可见组织选择不能超出其组织数据范围；系统管理员可在新建提示词弹窗直接发布公共提示词。
- 组件与数据：复用现有筛选器、卡片、文本列表、Button 和 Portal 弹窗，不新增公共组件；演示内容继续只存放在 `src/data/demo/`。
- 验证结果：源码 ESLint 0 error（保留 5 条既有图片性能 warning）；`next build --webpack` 通过并生成 28 个页面；素材与提示词的逐级提交、取消、审批、驳回、删除权限提示、状态筛选和四入口选择器已逐项浏览器核对；素材页 390px、提示词页 1280px/1024px/390px 无横向溢出，窄屏弹窗头尾可见且正文内部滚动，浏览器控制台无应用错误。
- 关联文件：`src/app/materials/MaterialsClient.jsx`、`src/app/prompts/PromptsClient.jsx`、`src/data/demo/materials.js`、`src/data/demo/prompts.js`、`docs/素材库提示词库权限管理PRD_260810.md`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-15：公共发布双视图与专家模式输入组件补全

- 状态：已按确认规则完成前端交互原型与全流程验收；真实审批服务、资源接口、服务端权限和生成尺寸能力尚未据此完成，不表示已部署或上线。
- 公共发布：素材与提示词公共审批通过后不再从团队范围迁移，原团队记录保留并显示“公共已发布”，公共范围引用同一资源并显示“公共可用”；团队入口保留原组织范围，公共入口展示全公司范围，不复制资源 ID。
- 权限规则：已公共发布的团队资源不能再次申请公共发布；原发布人和团队负责人不能编辑、删除，团队与公共入口均仅系统管理员管理。删除确认按全公司影响范围提示，历史作品或任务不受影响。
- 状态样例：素材的图片、视频、音频均提供“公共已发布”示例；提示词提供团队“公共已发布”示例。状态筛选、卡片、详情、团队/公共计数和审批通过后的即时结果保持一致。
- 专家模式：继续复用 `WorkbenchParameterSelect`，补齐宽度输入、按比例计算高度、智能比例禁用、尺寸校验、摘要与清空重置；新增 `WorkbenchPromptEditor`，统一编辑/预览、提示词模板、AI 润色、字数和输入框内清空，顶部操作靠右。
- 验证结果：相关源码 ESLint 0 error；Next.js 生产构建通过并生成 28 个页面；浏览器完成素材和提示词公共审批通过的双视图闭环，专家模式尺寸联动为 `1024×1024px`，输入框内清空与禁用状态正常；1280px、1024px、390px 无横向溢出，窄屏提示词弹窗和参数弹层保持视口内滚动，控制台无警告或错误。
- 关联文件：`src/app/materials/MaterialsClient.jsx`、`src/app/prompts/PromptsClient.jsx`、`src/app/image-tools/expert/ExpertWorkbench.jsx`、`src/components/workbench/WorkbenchPromptEditor.jsx`、`src/data/demo/materials.js`、`src/data/demo/prompts.js`、`docs/素材库提示词库权限管理PRD_260810.md`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-15：素材与提示词页签状态及按钮规则集中补全

- 状态：业务规则已确认并同步正式 PRD V1.6；本次只更新产品文档，不代表前端原型、服务端、部署或上线已完成对应改造。
- 来源页签：团队审批通过后，个人页签保留同资源来源记录并显示“团队已发布”，团队页签显示“团队可用”；公共审批通过后，团队页签显示“公共已发布”，公共页签显示“公共可用”。
- 文档结构：状态链路、角色口径、素材和提示词按钮矩阵、审批入口、顶部按钮、公共选择器及删除联动统一集中在 PRD 7.6 节，其他章节只引用该唯一完整口径。
- 范围边界：不包含专家模式或其他 AI 生成工具的页面、参数及生成流程。
- 关联文件：`docs/素材库提示词库权限管理PRD_260810.md`、`PROJECT_MEMORY.md`。

### 2026-08-15：工作台提示词输入交互简化

- 状态：已按确认方案完成前端公共组件调整；仅代表前端原型更新，不表示真实 AI 润色或生成服务已部署、上线。
- 当前规则：工作台提示词输入框始终可填写和编辑，删除“编辑 / 预览”切换；此前专家模式中关于提示词编辑/预览双模式的记录由本条规则替代。
- 公共组件：继续使用 `WorkbenchPromptEditor` 作为 AI 工作台创作提示词输入区，不新增另一套组件；提示词模板和 AI 润色位于顶部右侧，清空位于输入框右下角，空内容时清空和 AI 润色禁用。
- 复用边界：组件面向具备提示词模板或 AI 润色能力的生成工作台，不替代提示词库内容维护、系统提示词配置、审批意见等普通文本输入；其他工作台后续随页面优化逐步接入，本次不批量迁移。
- 文档边界：本次只同步 UI 规范和项目记忆，不修改素材库/提示词库权限 PRD。
- 关联文件：`src/components/workbench/WorkbenchPromptEditor.jsx`、`src/app/image-tools/expert/ExpertWorkbench.jsx`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-15：公共组件目录与工作台文本编辑器复用

- 状态：已按确认方案完成并验证前端原型实现；不表示真实 AI 润色、素材服务或生成服务已部署、上线。
- 组件目录：`/ui-guide` 新增“UI 规范 / 公共组件”顶部页签，公共组件视图支持 `?view=components` 深链、搜索和三层目录筛选，展示 `src/components` 下 21 个真实公共组件文件的路径、职责、使用页面、成熟度及交互演示。
- 登记规则：`src/config/component-catalog.js` 是公共组件目录的统一登记表；公共组件新增、删除、重命名或使用范围变化时必须同步登记，页面局部组件不计入。
- 编辑器体系：新增 `WorkbenchTextEditor` 负责通用工作台文本输入、字数、最大长度、输入框内清空、顶部工具栏和业务扩展区；`WorkbenchPromptEditor` 基于它组合提示词模板与 AI 润色，不保留编辑/预览切换。
- 页面接入：专家模式和 AI 买家秀复用 `WorkbenchPromptEditor`；AI 买家秀同步接入公共 `PromptPickerModal`；AI 商品套图的卖点文案复用 `WorkbenchTextEditor`，保留 500 字限制、AI 润色、语言选择和辅助说明。
- 后续规则：新增 AI 工作台子页面优先评估并复用现有工作台骨架、选择器和文本编辑器；只有现有组件职责无法覆盖且存在明确复用价值时，才按共享组件确认流程新增组件。
- 文档边界：本次同步 UI 规范和项目记忆，不修改素材库/提示词库权限 PRD。
- 验证结果：全量源码 ESLint 0 error（保留 5 条既有图片性能 warning）；`next build --webpack` 通过并生成 28 个页面；组件登记表与 `src/components` 均为 21 个文件且路径一一对应；浏览器验证 Top Tab 深链、目录搜索筛选、下拉菜单、模型/参数弹层、素材/提示词选择器、图片预览和编辑器输入联动，修复 Base UI 菜单分组契约后干净页面控制台 0 warning、0 error；`/ui-guide?view=components`、专家模式、AI 买家秀和 AI 商品套图在 1280px、1024px、390px 均无横向溢出，窄屏选择弹窗保持视口内固定头尾和正文滚动。
- 关联文件：`src/app/ui-guide/page.js`、`src/app/ui-guide/PublicComponentsCatalog.jsx`、`src/config/component-catalog.js`、`src/components/workbench/WorkbenchTextEditor.jsx`、`src/components/workbench/WorkbenchPromptEditor.jsx`、`src/app/image-tools/expert/ExpertWorkbench.jsx`、`src/app/ai-hub/BuyerShowPage.jsx`、`src/app/ai-hub/SuitePage.jsx`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-15：基础 UI 组件适配平台规范

- 状态：已按确认方案完成 `Button`、`Avatar`、`DropdownMenu` 三个基础公共组件的样式适配与浏览器验收。
- 按钮规则：移除未映射的 shadcn 默认主题类，统一山茶红主按钮、白底描边、品牌浅色、幽灵、危险和链接变体；默认高度 36px，小尺寸 32px、大尺寸 40px，统一 8px 圆角、焦点环和明确禁用态。
- 头像规则：回退头像使用中性浅灰底和深色文字；状态点使用品牌色与白色分隔环；头像组使用白色叠放分隔，避免出现透明空圆环。
- 菜单规则：菜单浮层默认最小宽度 160px，不再强制与触发器等宽；统一白底、8px 圆角、细边框和控制阴影，菜单项最小高度 36px，并补齐普通、禁用、危险及分组状态。
- 演示与验证：公共组件目录补齐按钮变体与尺寸、图片/回退头像及状态点、菜单普通/禁用/危险项演示；受影响文件定向 ESLint 0 error，Next.js 生产构建通过并生成 28 个页面；浏览器验证窄屏和 1280px 布局、菜单键盘关闭及顶部个人菜单均正常。
- 关联文件：`src/components/ui/button.jsx`、`src/components/ui/avatar.jsx`、`src/components/ui/dropdown-menu.jsx`、`src/app/ui-guide/PublicComponentsCatalog.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-15：主体替换与工作台近期历史布局

- 状态：已按确认方案完成前端交互原型；真实生成任务、历史记录接口、服务端权限、部署和上线尚未据此完成。
- 近期历史：新增公共 `WorkbenchRecentHistory`，在结果面板右侧直接展示当前功能最近 3 天历史，不分页、栏内滚动，保留缩略图、状态、时间、提示词摘要、复制提示词、继续编辑、刷新和完整历史入口；窄屏改为可展开区域。
- 结果布局：专家模式和主体替换统一使用“结果主区 + 280px 历史窄栏”。结果面板只保留外层标题、说明和任务状态，删除内部重复标题、状态摘要和请求 ID、模型、任务类型、参考图数量信息行。
- 主体替换：完成 `/image-tools/subject-replace` 工作台，采用 3:7 双栏；最多 14 张图片且图一为原图、其余为主体参考；复用素材选择、本地上传、拖拽排序、大图预览、替换、删除、提示词模板、AI 润色、模型和参数公共组件，不提供替换范围或区域编辑。
- 参数与任务：支持线上确认的 5 个模型、1K/2K/4K、智能比例及 7 个固定比例、1～9 张输出，包含提交禁用、清空、异步进度、空/成功/失败结果结构、下载、近期历史和完整历史筛选入口。
- 公共组件目录：`WorkbenchRecentHistory` 已登记到 `src/config/component-catalog.js`，并在 `/ui-guide?view=components` 提供真实组件演示；公共组件总数由 21 增至 22。
- 验证结果：定向及全量源码 ESLint 0 error（保留 5 条既有图片性能 warning）；图片尺寸测试 4/4 通过；Next.js 生产构建通过并生成 28 个页面；浏览器验证专家模式与主体替换在 1280px、1024px、390px 均无横向溢出，390px 最近历史默认折叠且可展开。主体替换素材弹窗包含个人/团体/公共/本地及固定类目，提示词弹窗包含个人/团队/公共/灵感广场；素材回填、提交禁用、生成进度、成功结果和下载入口可用，公共组件目录登记与演示正常，页面控制台无应用错误。
- 关联文件：`src/components/workbench/WorkbenchRecentHistory.jsx`、`src/app/image-tools/expert/ExpertWorkbench.jsx`、`src/app/image-tools/subject-replace/SubjectReplaceWorkbench.jsx`、`src/app/image-tools/subject-replace/page.js`、`src/data/demo/subject-replace.js`、`src/config/component-catalog.js`、`src/app/ui-guide/PublicComponentsCatalog.jsx`、`UI_SPEC.md`、`PROJECT_MEMORY.md`。

### 2026-08-15：产品微调工作台

- 状态：已按确认方案完成 `/image-tools/product-refine` 前端交互原型并通过浏览器验收；真实生成接口、积分扣减、服务端权限、持久化历史、部署和上线尚未据此完成。
- 页面与素材：页面由占位页升级为 3:7 工作台，一次处理 1 张 JPG/PNG/WEBP 商品图；复用公共素材选择器，支持个人、团体、公共和本地来源，并保留固定一级类目、预览、替换、删除及区域编辑。
- 提示词与参数：提示词始终可填写和编辑，不提供编辑/预览切换；复用公共提示词编辑器和模板选择器，模板来源为个人提示词、团队提示词、公共提示词和灵感广场。保留 5 个模型、1K/2K/4K、智能比例及 7 个固定比例、1～9 张输出。
- 任务与结果：支持未开始、处理中、已完成、失败和已终止状态；处理中可终止，终止后保留图片、区域和提示词并可再次提交。结果区保留原图/AI 输出对照、失败重试和单图下载，不展示请求 ID、模型、水印或重复状态摘要。
- 近期历史：复用 `WorkbenchRecentHistory` 展示当前功能最近 3 天记录，支持刷新、复制提示词、继续编辑和进入完整历史；组件目录使用范围已补充“产品微调”，公共组件总数不变。
- 验证结果：浏览器验证素材与提示词弹窗、单图上限、区域编辑、任务终止/再次提交、成功结果、下载入口、历史复制/继续编辑和清空链路可用；1280px、1024px、390px 均无横向溢出，390px 历史默认折叠且可展开，页面控制台无应用错误。
- 关联文件：`src/app/image-tools/product-refine/ProductRefineWorkbench.jsx`、`src/app/image-tools/product-refine/page.js`、`src/data/demo/product-refine.js`、`src/config/component-catalog.js`、`PROJECT_MEMORY.md`。

### 2026-08-16：批量美颜工作台

- 状态：已按线上实际能力完成 `/image-tools/batch-beautify` 前端交互原型并通过浏览器验收；真实批量生成接口、积分扣减、任务持久化、打包下载、部署和上线尚未据此完成。
- 页面与输入：页面由占位页升级为 3:7 工作台，一次最多处理 14 张 JPG/PNG/WEBP 人像图；复用公共图片队列和素材选择器，支持个人、团体、公共、本地来源及多图上传、排序、预览、替换、删除。
- 美颜与提示词：保留自然、标准、精修三个业务预设，删除与预设重复的处理强度下拉；切换预设同步推荐提示词，提示词仍可自由编辑。复用公共提示词编辑器和模板选择器，模板来源为个人提示词、团队提示词、公共提示词和灵感广场。
- 模型与参数：保留 5 个现有模型、1K/2K/4K、智能比例及 7 个固定比例；图片张数定义为每张输入图生成 1～9 张，页面同步展示预计结果总数，极限为 14×9=126 张。
- 任务与结果：结果按输入图片分组并默认只展开首组，覆盖未开始、待处理、处理中、成功、失败、部分成功和已终止状态；处理中可终止且保留输入与已有结果，失败项可单独重试，成功项支持单张下载和批量下载入口。通过任务轮次校验隔离已终止、清空或重新提交后的旧异步回调。
- 近期历史：复用 `WorkbenchRecentHistory` 展示当前功能最近 3 天记录，支持刷新、复制提示词、继续编辑和进入完整历史；公共组件目录使用范围已补充“批量美颜”，未新增共享组件。
- 验证结果：全量源码 ESLint 0 error（保留 5 条既有图片性能 warning）；图片尺寸测试 4/4 通过；`next build --webpack` 通过并生成 28 个页面。浏览器完成素材四来源与固定类目、提示词四来源、3 张输入×每张 3 张、部分成功、失败重试及输入保留链路核对；1280px、1024px、390px 无横向溢出，页面控制台无应用错误。
- 关联文件：`src/app/image-tools/batch-beautify/BatchBeautifyWorkbench.jsx`、`src/app/image-tools/batch-beautify/page.js`、`src/data/demo/batch-beautify.js`、`src/config/component-catalog.js`、`PROJECT_MEMORY.md`。

### 2026-08-16：历史记录筛选层级调整

- 状态：已按确认规则完成历史记录筛选区的前端布局调整；筛选数据、权限、接口和记录内容未变更。
- 一级范围：筛选区第一行只保留“我的记录 / 所有记录”，用于决定当前账号或全员记录范围；记录总数在同一行右侧展示，并按全部筛选条件实时计算。
- 二级功能：全部功能节点移至一级范围下方，统一归入“功能类型”二级筛选；切换范围时保留当前功能条件，工作台携带 `source` 参数进入历史记录时仍能直接选中对应功能。
- 关联文件：`src/app/history/HistoryClient.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-16：数据智能一期企业历史基线

- 状态：已按确认方案完成 `/admin/data` 可点击前端原型并通过浏览器验收；页面数据为截至 2026-07-30 的一次性历史快照，不代表实时数据，真实报表接口、新埋点验收、部署和上线尚未据此完成。
- 一期范围：展示注册用户、实际生图用户、任务用户覆盖率、累计任务、成功任务、累计积分和平均每任务积分；展示任务状态构成、功能任务量/用户数排行，以及 16 项功能的任务数、用户数、成功、失败、取消和未完成明细。
- 数据口径：累计积分以任务快照 136,167 为主数值；账号同步积分 105,452 仅在指标说明中作为参考，两者相差 30,715，当前材料不足以解释差异，不据此推算人民币成本或节省工时。功能名称保留快照原始标识，技术型功能不归并。
- 交互与边界：支持任务状态切换、任务数/用户数排行切换、明细列排序和指标说明弹窗；无日期序列、组织、结果采纳、耗时、失败原因和资产复用字段，因此一期不展示趋势、部门/个人下钻、采纳率、平均耗时、失败原因、资产复用或单有效产出成本。
- 响应式规则：1280px 与 1024px 使用桌面明细表，736px 与 360px 使用移动卡片明细；四档宽度均无页面横向溢出，360px 指标说明弹窗保持在视口内并仅滚动正文。
- 验证结果：快照 16 项功能汇总与总任务 49,512、成功 43,246、失败 1,096、取消 5,165、未完成 5 逐项一致；定向 ESLint 0 error；`next build --webpack` 通过并生成 28 个页面；浏览器验证排行切换、状态切换、明细排序和指标说明交互正常，页面控制台 0 warning、0 error。
- 关联文件：`src/app/admin/data/page.js`、`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-16：数据智能按组织下钻线稿重构

- 状态：已按用户重新确认的最终线稿结构重做 `/admin/data` 前端交互原型；本条覆盖同日旧版“独立快照看板”的页面结构，但不改变 2026-07-30 历史快照的数据事实与非实时边界。
- 页面结构：恢复数据总览、使用分析、任务与质量、成本效率、资产与复用、数据明细六个分析主题，以及时间/功能/模型/组织筛选、全公司到部门和人员的范围面包屑、四项主题指标、趋势与贡献区、部门/人员统计排行和主题说明带。
- 数据展示：快照已支持的注册用户、实际生图用户、任务覆盖率、任务状态、16 项功能明细和累计积分按对应主题展示真实值；快照不支持的日期序列、组织、人员、模型、结果采纳和资产复用等字段统一显示 `--`，不以演示数值伪装为已获得数据。
- 原型交互：支持六个分析主题切换、快照/近 30 天/近 7 天/本月切换、功能筛选、部门/人员统计切换、组织搜索、全公司 → 一级部门 → 子部门/人员的演示层级下钻与面包屑返回；切换到无数据时间或组织范围后，指标同步显示 `--`。
- 响应式边界：桌面使用线稿中的高密度表格和双栏分析区，窄屏改为主题网格、单列筛选与指标、纵向分析区和统计卡片；未新增共享组件或图表依赖。
- 验证结果：定向 ESLint 0 error；全量 `src` ESLint 0 error，保留 5 条既有图片性能 warning；16 项功能任务状态汇总逐项匹配快照总数；`next build --webpack` 通过并生成 28 个页面。浏览器自动视觉复验因当前本地页接管被浏览器安全策略阻止，未绕过策略，仍需人工查看本地原型确认视觉效果。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-16：数据智能组织范围改为顶部统一控制

- 状态：已按用户反馈修正 `/admin/data` 前端原型的组织范围交互；本次仅调整原型状态流，不代表真实组织报表接口或组织数据已接入。
- 全局范围：筛选区首项统一为“数据范围”，支持原型演示中的全公司、一级部门、子部门和人员层级；切换范围后，核心指标、趋势、构成、功能表现和组织统计统一读取当前范围。
- 层级返回：当前范围面包屑保留父级返回能力；切换六个分析主题时保留当前组织范围，不再自动回到全公司。
- 排行边界：底部“按部门统计 / 按人员统计”仅改变当前范围下的统计对象，不改变顶部数据范围；部门和人员名称改为纯展示，删除“查看下级”“查看个人”等由底部反向控制全局数据的入口。
- 数据边界：组织选项仍为 `src/data/demo/` 中的原型占位层级，2026-07-30 快照没有组织字段，因此部门、子部门和人员范围的指标继续显示 `--`，不伪装为真实组织数据。
- 验证结果：定向 ESLint 0 error；全量 `src` ESLint 0 error，保留 5 条既有图片性能 warning；`next build --webpack` 通过并生成 28 个页面。浏览器实测顶部范围切换后指标统一联动、主题切换保留范围、底部统计对象切换不改变范围、面包屑可返回父级；1280px、1024px、736px、360px 均无页面横向溢出，桌面与手机顶部筛选无重叠。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-16：数据智能全局时间层级调整

- 状态：已按用户反馈调整 `/admin/data` 原型的信息层级；本次只调整前端筛选布局与交互复验，不改变数据口径、权限或组织占位边界。
- 全局条件：时间范围与数据范围共同放在六个分析主题上方，表示两者统一作用于全部主题；时间范围在前，组织范围在后。
- 主题条件：功能和模型保留在主题标签下方，作为当前分析主题内的筛选条件；当前主题说明随标签切换更新。
- 点击状态：页面无全屏遮罩或失效事件处理器，六个主题、时间、组织和功能筛选均需在最新客户端页面中可操作；本地开发页修改后统一刷新并释放浏览器控制，避免旧热更新页面继续显示但交互未恢复。
- 验证结果：定向 ESLint 0 error；全量 `src` ESLint 0 error，保留 5 条既有图片性能 warning；`next build --webpack` 通过并生成 28 个页面。浏览器刷新后实测时间、组织、主题、功能筛选和指标说明弹窗均可操作；构建后再次点击主题切换正常，无全屏点击拦截层；1280px、1024px、736px、360px 均无页面横向溢出，四档均保持“全局条件 → 主题标签 → 主题条件”的顺序。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-16：数据智能查询条件统一置顶

- 状态：已按用户反馈调整 `/admin/data` 前端原型的查询层级；本次仅合并筛选布局，不改变快照口径、组织下钻、主题内容或演示数据边界。
- 查询结构：时间范围、数据范围、功能和模型统一放在六个分析主题标签上方，作为作用于当前报表结果的同一组查询条件，不再拆分为“全局筛选”和“主题筛选”。
- 页面顺序：统一查询区 → 六个分析主题标签 → 当前数据范围与分析结果；查询区在桌面使用四列、平板使用两列、手机使用单列，避免横向溢出。
- 交互边界：时间、数据范围、功能和主题切换继续沿用现有状态逻辑；模型当前仍只有“全部模型”占位，不代表模型维度数据已接入。
- 验证结果：定向 ESLint 0 error；全量 `src` ESLint 0 error，保留 5 条既有图片性能 warning；`next build --webpack` 通过并生成 28 个页面。浏览器刷新后实测时间、数据范围、功能和主题切换正常，并已恢复默认查询状态；1280px、1024px、736px、360px 均无横向溢出，四项查询条件均保持在顶部查询区内。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-18：数据智能管理员报表一期收敛

- 状态：已按确认方案完成 `/admin/data` 管理员报表前端原型并通过本地验证；页面仍使用截至 2026-07-30 的演示快照，真实报表接口、服务端数据权限、埋点验收、部署和上线尚未据此完成。
- 用户与范围：一期首要用户为系统管理员，需要判断全公司平台使用情况、平台任务运行效率和积分使用合理性。本期不实现部门负责人权限；后续不拆全公司版与部门版两套页面，而是在同一页面架构下注入角色可见的数据范围。
- 页面结构：统一为数据总览、使用分析、任务与效率、积分使用、资产与复用、数据明细六个主题；删除虚假的部门/个人范围、组织下钻和无数据时间选项，数据范围固定显示“全公司”，功能筛选继续联动当前主题。
- 真实数据展示：保留注册用户、实际任务用户、用户覆盖率、累计任务、任务状态、16 项功能明细、任务快照累计积分和账号同步积分；新增任务量/用户数排行、任务状态环图、积分双口径对比和管理关注项。取消任务占比、积分差异和专家模式任务集中度只作为待核查信号，不直接解释为浪费、提效或业务 ROI。
- 数据补充边界：下一阶段优先增加任务开始/完成时间、统一模型标识、任务与积分流水关联。任务处理时长、异常任务净消耗、模型维度和资产复用暂无可靠数据时统一显示“数据积累中”；本期不展示人民币成本、完整留存、完整复用率、单有效产出成本或人工节省工时。
- 视觉与交互：沿用山茶红主题与现有设计 token，采用白底、轻描边、弱阴影、8px 圆角、紧凑 KPI、高密度表格和移动端卡片；未新增共享组件、路由、图表依赖或权限实现。
- 验证结果：定向及全量 `src` ESLint 0 error（保留 5 条既有图片性能 warning）；生产构建通过并生成 28 个页面；生产模式浏览器实测六主题、功能筛选和指标说明弹窗可用，1280px、1024px、736px、390px 无页面横向滚动，390px 自动切换单列指标与卡片明细，页面控制台无应用错误。根目录 `eslint .` 会误扫嵌套工程的 `.next/dev` 生成文件并报第三方构建代码错误，本次未修改该无关目录。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-18：数据智能13功能、组织范围与实际使用口径调整

- 状态：已按用户确认规则完成 `/admin/data` 前端原型调整并通过本地验证；真实组织、模型、AI 提示词和 AI 无限画布数据接口及服务端权限尚未据此完成。
- 功能口径：线上 13 个工具统一进入功能筛选。原始 16 条任务类型归并为 11 个有快照任务数据的业务功能，AI 买家秀合计 6,044 条任务，AI 区域重绘合计 157 条任务；AI 提示词和 AI 无限画布作为正式功能展示暂无统计结果。归并后 49,512 条任务及成功、失败、取消、未完成数量与原快照逐项守恒，页面不展示 `ai-capability:*`、买家秀分镜/评价或技术标识。
- 使用口径：页面访问不计入实际使用。生图、视频和无限画布以后台成功创建任务为一次实际使用，后续失败或取消仍计入使用；AI 提示词以生成、润色或反推等核心动作成功为一次实际使用。成功任务、成功结果和采纳动作单独统计。AI 提示词不扣积分，积分页按“不扣积分”展示。
- 组织范围：数据范围复用 `src/data/demo/admin.js` 的现有组织树，系统管理员可选择全公司、部门和下级组织；2026-07-30 快照仅支持全公司，切换其他组织后统一显示真实空状态，不填充演示指标。后续部门负责人继续使用同一页面，并由服务端按授权组织及下级范围过滤。
- 模型边界：删除无模型数据支撑的“全部模型 / 数据积累中”占位及页面内相关模型准备项。研发确认任务模型标识、历史完整性、跨功能命名和组合查询可用后再恢复真实模型筛选。
- 验证结果：定向 ESLint 0 error；全量 `src` ESLint 0 error，保留 5 条既有图片性能 warning；生产构建通过并生成 28 个页面。数据脚本验证 13 个功能中 11 个任务功能汇总与快照总数一致；生产页面验证13功能筛选、组织切换、AI 提示词积分规则、AI 无限画布任务状态和数据明细归并结果可用，1280px、1024px、736px、390px 无横向溢出，390px 指标弹窗保持视口内，页面控制台无应用错误。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-18：数据智能时间与组织筛选原型展示调整

- 状态：已按用户确认方案完成 `/admin/data` 纯前端演示原型调整；时间、组织和功能筛选用于展示目标交互，切换后继续沿用现有演示指标，不代表已接入对应日期或组织的真实统计数据。
- 页面信息：删除固定快照日期、累计快照、数据来源文件等展示，并确认页面不出现“接口待接”“后续补字段”“数据积累中”等开发备注；原型成熟度继续由 `PageShell` 的“原型中”状态表达，数据和技术边界仅记录在项目文档中。
- 时间筛选：采用今日、近 7 天、近 30 天、自定义和开始/结束日期；快捷日期按 `Asia/Shanghai` 计算，手动修改日期进入自定义状态，起止日期顺序自动纠正。
- 组织筛选：复用 `src/data/demo/admin.js` 的组织树，以页面局部 Portal 浮层支持层级展开、完整路径搜索、单选、外部点击和 Esc 关闭，并根据视口自适应定位。搜索状态强制展示命中路径并同步展开箭头，避免节点已展开但图标仍呈收起状态。
- 权限边界：本期不实现部门负责人权限、服务端角色过滤或真实组织聚合；后续仍在同一报表架构中根据现有组织数据权限注入可见范围，不拆分管理员版与部门版页面。
- 验证结果：全量 `src` ESLint 0 error（保留 5 条既有图片性能 warning）；`next build --webpack` 通过并生成 28 个页面；13 个功能中 11 个有任务数据，任务总数及成功、失败、取消、未完成五项汇总守恒；生产页面实测时间快捷项、手动日期、组织展开/搜索/选择/完整路径及浮层关闭可用，1280px、1024px、390px 无页面横向溢出，390px 组织浮层完整位于视口内。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-18：数据智能完整展示型虚拟数据补全

- 状态：已按用户要求将 `/admin/data` 调整为完整展示型前端原型；原缺失指标统一补充结构合理的虚拟数据，所有新增数字仅用于演示，不代表真实经营数据、埋点、接口、权限、验收或上线能力。
- 数据范围：补齐 AI 提示词核心动作、AI 无限画布任务、各功能积分和平均处理时长，并新增全局效率、取消原因、积分差异构成、素材入库、复用、继续编辑、下载及团队/公共发布等演示指标。
- 统计关系：全功能实际使用数同时包含任务型使用和 AI 提示词核心动作；任务状态只汇总任务型功能。13 个功能均具备使用用户、运行状态、覆盖率、处理时长和积分字段，AI 提示词按核心动作展示且保持不扣积分。功能积分合计与总积分一致，任务型功能的成功、失败、取消和未完成合计与总任务状态一致。
- 页面展示：六个主题均取消可见空数据状态；任务与效率改为状态环图加效率明细，积分使用支持总览、收费功能和 AI 提示词三种展示，资产与复用改为指标卡、行为对比和发布概览。数据明细统一展示任务与核心动作的成功、取消、失败和成功率。
- 边界：虚拟数据继续集中在 `src/data/demo/data-intelligence.js`，页面不增加接口或开发备注；后续接入真实数据时需要整体替换并重新验证口径，不能沿用原型数字形成业务结论。
- 验证结果：定向 ESLint 0 error；全量 `src` ESLint 0 error（保留 5 条既有图片性能 warning）；`next build --webpack` 通过并生成 28 个页面；脚本验证 13 个功能无缺失字段，实际使用、任务状态和积分汇总守恒；生产页面实测六主题、AI 提示词、AI 无限画布及功能积分均有数据，1280px、1024px、390px 无横向溢出，页面控制台无应用错误。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-18：数据总览管理关注项与部门价值概览调整

- 状态：已按用户确认方案完成 `/admin/data` 的“数据总览”主题调整并通过本地验证；本次只调整数据总览，其他分析主题保持现状。
- 管理关注项：固定展示产出后续使用率、首轮可用率、跨任务复用率、平均每个后续使用结果积分，分别观察结果是否进入后续工作、创作链首轮是否获得可用结果、生成素材是否进入其他任务以及积分投入效率；不再展示覆盖基线、取消任务结构、积分差异构成和专家模式集中度。
- 部门概览：总览默认删除重复的主要功能明细，改为“各部门使用与价值概览”，仅展示当前统计范围内有实际使用记录的部门，包含实际使用人数、实际使用次数、人均实际使用、产出后续使用率、净消耗积分和平均后续使用结果积分；不使用组织总人数计算覆盖率，也不对无使用部门作低效或异常评价。
- 原型数据：管理关注项和部门数据均为 `src/data/demo/` 中的展示型虚拟数据；部门人数、实际使用次数和净积分合计与总览演示总数一致，不代表真实经营数据。选择单个功能时继续展示该功能自身明细，避免用全功能部门数据响应单功能筛选。
- 验证结果：相关文件定向 ESLint 0 error；数据脚本验证部门实际使用人数 266、实际使用次数 51,326、净积分 136,167 与总览演示值一致；Webpack 开发模式浏览器实测桌面表格和 390px 移动卡片正常，390px 页面无横向溢出。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-19：数据报表指标说明与功能排行命名调整

- 状态：已按页面评审意见完成 `/admin/data` 局部交互与文案调整，未改变演示数据和既有统计口径。
- 指标说明：指标卡帮助入口改为只展示当前被点击指标的“指标含义”和“统计规则”，不再混合展示其他指标、积分口径或全局判断边界；实际使用用户继续按当前统计范围内至少完成一次实际使用的用户去重统计，页面访问不计入。
- 排行区分：“实际使用排行”更名为“功能使用次数排行”，用于观察各功能使用规模；“使用用户排行”更名为“功能使用人数排行”，按去重用户数观察各功能覆盖广度。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-19：积分使用分析与管理决策原型调整

- 状态：已按用户确认方案完成 `/admin/data` 的“积分使用”主题前端原型调整；本次只调整积分相关展示与演示数据，不代表真实积分流水、埋点、接口或权限已经接入。
- 统一口径：积分报表只使用当前统计范围内已经实际扣减并完成结算的消耗积分；删除账号同步积分、双口径差异、同步延迟和差异构成等基于不同时间快照的错误展示。积分申请、账户余额、补发和退还不进入本页消耗积分统计。
- 页面结构：核心指标统一为消耗积分、积分使用人数、平均每次使用积分和平均每个后续使用结果积分；新增积分趋势、积分管理信号、按功能统计、按部门统计和人员积分使用 TOP N。
- 决策边界：积分管理信号只提示积分增速与使用增速、人员积分集中度和高积分低后续使用等需要复核的情况，不直接判断积分浪费。人员排行用于定位高频用户和实际使用场景，不作为员工绩效排名。
- 数据联动：功能、部门和趋势演示积分均汇总为 136,167；趋势扣积分使用次数汇总为 49,998，后续使用结果汇总为 21,384。AI提示词继续作为不扣积分功能单独展示，不进入积分排名。
- 权限边界：本期仍不实现服务端数据权限；后续部门负责人复用同一页面结构，由现有组织数据权限限制可查看的部门、人员和明细范围。
- 验证结果：相关文件定向及全量 `src` ESLint 0 error（保留 5 条既有图片性能 warning）；`next build --webpack` 通过并生成 28 个页面；数据脚本验证功能、部门、趋势、后续使用结果和 TOP 10 集中度关系一致。浏览器实测总积分、单功能、AI提示词不扣积分、指标说明和 TOP N 交互可用，1280px 三张数据表无内部横向滚动，390px 无页面横向溢出，页面控制台无应用错误。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-19：资产与复用分析原型调整

- 状态：已按用户确认方案完成 `/admin/data` 的“资产与复用”主题前端原型调整；本次只调整资产相关展示与演示数据，不代表真实资产埋点、接口、组织权限、验收或上线已经完成。
- 统计范围：资产统一包含素材库与提示词库，支持整体概览、个人/团队/公共资产范围、团队贡献与使用、热门素材和热门提示词等维度；灵感广场及站外行为不纳入统计。
- 实际使用口径：素材以成功选入平台任务为一次使用；提示词以平台内复制成功或确认使用为一次使用，成功带入工具单独作为深层使用。页面访问、浏览、收藏和未完成动作不计入实际使用。
- 管理决策：核心指标展示可用素材数、素材复用率、可用提示词数和提示词使用率；管理信号只提示高价值待推广、长期未使用和新增后未使用的资产，便于公共库推广与存量治理，不自动判断资产质量或业务价值。
- 原型数据：新增资产总量、活跃量、使用次数、团队、范围和热门资产数据均为 `src/data/demo/` 中的脱敏虚拟数据，并保持范围汇总与整体数据一致；功能筛选只联动热门资产排行，整体、范围和团队区继续作为展示型原型数据。
- 权限边界：本期不实现服务端数据权限；后续部门负责人复用同一页面结构，由现有组织数据权限限制可查看的资产范围、团队和排行，跨团队使用需按使用事件发生时的组织快照统计。
- 验证结果：相关文件定向 ESLint 0 error；全量 `src` ESLint 0 error，保留 5 条既有图片性能 warning；`next build --webpack` 通过并生成 28 个页面；数据脚本验证素材、提示词、资产总量、活跃量和使用次数汇总一致。浏览器实测四项核心指标、素材与提示词概览、资产管理信号、范围/团队统计、两类热门排行、功能筛选、空状态和指标说明均可用；1280px、390px 无页面横向溢出，1024px 宽表格使用局部横向滚动，390px 自动切换卡片布局且弹窗保持在视口内。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-19：数据智能报表 UI 规范修正

- 状态：已完成 `/admin/data` 六个分析主题的 UI 规范审计与修正；本次只调整视觉样式，不改变指标、演示数据、筛选、权限边界或交互规则。
- 根因与修复：5 个 `divide-x/divide-y` 容器只在父级设置 `borderColor`，实际分隔线位于子元素并回退为正文色，形成 18 条深色线；现已直接使用 `divide-[var(--border-light)]`，六个主题的异常深色边框扫描结果均为 0。
- 面板规范：查询区、指标卡、图表、榜单、表格及管理信号等主面板统一复用全局 `xm-panel`，计算样式统一为 `--border-base` 描边、16px 圆角和 `--shadow-card`，不再使用局部 `rounded-lg + shadow-sm` 旧组合。
- 验证结果：相关文件定向 ESLint 0 error；全量 `src` ESLint 0 error，保留 5 条既有图片性能 warning；`next build --webpack` 通过并生成 28 个页面；浏览器逐主题扫描 1280px 下边框颜色、圆角和阴影，1024px、390px 下六主题均无整页横向溢出或文字越界。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-19：数据智能人员洞察原型调整

- 状态：已按用户确认方案将 `/admin/data` 的“数据明细”主题替换为“人员洞察”，并完成本地前端原型验证；本次使用脱敏虚拟人员数据，不代表真实人员埋点、接口、组织权限、验收、部署或上线已经完成。
- 页面目标：管理员可按姓名或部门搜索有实际使用记录的人员，查看个人的平台使用情况与产出价值；不再重复展示功能汇总表，不提供任务流水、综合评分、人员价值排名或绩效判断。
- 页面结构：展示人员基本情况、实际使用次数、活跃天数、使用功能数、主要使用功能、产出后续使用率、首轮可用率、跨任务复用资产、团队使用的素材与提示词、积分效率、功能使用结构和七日趋势；积分和实际使用支持与同部门有记录人员均值对比。
- 筛选与权限边界：顶部时间、组织和功能筛选继续作用于人员洞察；人员候选只保留当前条件下有实际使用记录者。后续部门负责人沿用同一页面结构，由服务端组织数据权限限制可查询人员；本期未实现服务端权限。
- 判断边界：管理参考只陈述功能集中、资产复用和单位后续使用结果积分等当前数据事实，不自动判断人员浪费、低效、价值高低或绩效表现。
- 原型数据：新增 10 名 `成员 Axx` 脱敏虚拟人员，功能与七日趋势中的实际使用次数、积分和后续使用结果均与个人汇总守恒；所有原型数据集中在 `src/data/demo/data-intelligence.js`。
- 验证结果：相关文件定向 ESLint 0 error；人员数据守恒检查通过；`next build --webpack` 通过并生成 28 个页面；本地生产页面验证人员搜索、功能与组织筛选联动、空状态可用，1280px、1024px、390px 无整页横向溢出，390px 详情区无文字溢出，页面控制台无错误。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-19：数据智能人员指标口径与埋点评估规则

- 状态：已按用户确认方案调整 `/admin/data` 人员洞察原型；本次只更新原型与项目规则，不输出数据智能 PRD，待其余报表需求全部确认后统一编写。
- 评估基线：以后新增或调整报表指标时，必须先对照知识库 `先马AI设计平台_埋点需求PRD_V1.0_2026-07-27.md` 判断可落地性；原 PRD 已覆盖的事件和字段标记为“现有埋点支持”，只有缺失部分才标记为“新增埋点”。最终数据智能 PRD必须单独汇总新增埋点，不把原型虚拟数据误写成已具备的数据能力。
- 指标口径：“活跃天数”调整为“有效活跃天数”，按上海自然日去重，生成、采纳或资产复用等有效业务行为至少发生一次即计 1 天，登录、浏览、搜索、预览和失败动作不计；该指标由现有埋点支持。
- 指标口径：“首轮可用率”调整为“首轮直接采纳率”，定义为首轮结果被直接采纳的创作链数 / 首轮成功产出结果的创作链数；直接采纳包括下载、加入素材库、认可、复制/导出或带入其他任务，仅预览、无操作及后续轮次才采纳不计首轮直接采纳。
- 新增埋点边界：首轮直接采纳率需要补充 `creative_chain_id`、`round_index`、`source_result_id`；`asset_reuse` 需要明确 `task_id`；判断素材或提示词被团队使用需要补充 `owner_user_id`、`owner_department_id`，或确认可稳定关联现有资源表中的等价字段。
- 页面交互：六个主题标签滚动到内容区顶部后吸顶；切换主题时保持在标签下方内容区，不返回顶部筛选区。人员页固定展示“功能使用、资产复用、积分效率”三维度数据总结；资产复用同时展示去重资产数和累计复用次数，不设置管理触发条件。
- 数据边界：上述页面数字继续使用 `src/data/demo/` 中的脱敏虚拟数据，仅用于原型展示，不代表新增埋点、真实接口、组织权限、验收、部署或上线已经完成。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`src/data/demo/data-intelligence.js`、`PROJECT_MEMORY.md`。

### 2026-08-19：数据智能趋势图时间轴规则

- 状态：规则已确认并完成前端原型调整；数据智能 PRD仍待全部需求确定后统一输出。
- 统一范围：积分使用的“积分使用趋势”和人员洞察的“使用趋势”必须读取顶部时间范围，不再固定展示最近 7 天。
- 聚合粒度：今日按小时展示；2～31 天按日展示；32～180 天按周展示；超过 180 天按月展示。两个趋势图共用同一套时间分桶和区间标题规则。
- 原型实现：两个趋势图共用同一页面内时间分桶函数和区间标题；柱数较多时图表区域支持局部横向滚动，演示数据按当前筛选范围重新分配且保持积分、实际使用次数和后续使用结果数汇总守恒。
- 数据边界：当前原型趋势数据仍为脱敏虚拟数据；原型完成不代表真实时间序列接口、埋点聚合、验收、部署或上线已经完成。

### 2026-08-19：数据智能积分产出规模与转化效率规则

- 状态：已按用户确认方案完成积分使用主题的前端原型调整；暂不输出 PRD，最终低样本阈值仍待 PRD统一确认。
- 字段口径：功能、部门、人员积分表统一使用“实际使用次数”“后续使用结果数”“平均每个后续使用结果积分”；后续使用结果数按结果 ID 去重，同一结果发生下载、继续编辑、加入素材库、认可或跨任务复用等多个动作只计 1 个。
- 部门统计：保留消耗积分、积分使用人数、实际使用次数、平均每次使用积分、后续使用结果数、平均每个后续使用结果积分，默认按后续使用结果数降序、消耗积分降序作为同值次序，不再按消耗积分排名。
- 人员统计：“人员积分使用排行”改为“有效产出贡献 TOP N”，默认按后续使用结果数降序、消耗积分降序作为同值次序；消耗积分只表示投入，平均每个后续使用结果积分只作效率参考，不作为员工绩效评价。
- 判断规则：有效产出规模 = 后续使用结果数；积分转化效率 = 消耗积分 / 后续使用结果数。原型对后续使用结果数少于 10 的功能、部门或人员标记“样本较少”，不自动判断低效或浪费。
- 埋点边界：基础计算继续依赖既有 `credit_settled` 及结果下载、继续编辑、加入素材库、认可等事件；跨任务复用完整归因仍依赖已记录的结果关联字段补充。本次没有新增 PRD文件。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-19：数据智能统计归属与指标说明调整

- 状态：已按用户确认规则更新 `/admin/data` 前端演示原型；暂不输出数据智能 PRD，待全部需求确认后统一编写。
- 时间与归属：产出价值和积分效率按结果生成时间归属，后续使用结果采用生成后 7 天观察窗；资产使用次数按后续行为发生时间归属。32～180 天从所选开始日期起每 7 天分桶，最后不足 7 天单独成桶；“注册用户覆盖率”保持现有展示。
- 字段统一：页面中的“平均每个后续使用结果积分”统一调整为“平均后续使用结果积分”；同一结果发生多个后续动作按结果 ID 去重只计 1 个，少于 10 个后续使用结果继续标记“样本较少”。
- 指标说明：顶部指标卡右上角帮助入口统一展示当前指标的“统计内容、统计规则、管理价值”，覆盖使用、任务运行、积分、资产和人员洞察相关指标；“积分规则”使用独立说明，明确 AI提示词免费能力与收费功能的统计边界。
- 弹窗样式：三段说明合并在一个整体描边容器内，以小标题和浅色分隔线组织内容，不再为每段单独套卡片。
- 数据边界：本次只调整页面文案、统计口径说明和字段命名，不修改演示数值；原型完成不代表真实接口、埋点验收、组织权限、部署或上线已经完成。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-20：无限画布静态原型接入项目

- 状态：已按用户确认方案完成 `/ai-canvas` 本地前端演示接入并通过验证；不代表后端、数据持久化、部署或上线已经完成。
- 接入方式：项目管理页复用当前项目的 `PageShell`、设计 Token、按钮、菜单、顶层弹窗和演示数据约定；点击项目后使用全屏同源 iframe 加载现有 7.8 无限画布静态原型，外层只桥接项目名称创建与返回项目列表。
- 原型边界：`public/prototypes/xianma-ai-canvas-v1.html` 与来源文件 SHA-256 均为 `CF2CDC726D6DDCD778764474DBF3DA73ACE565BBB920DA3DE838F8752F9214DA`，保持字节级一致；原型品牌图也通过固定哈希校验。未修改原画布源码、UI 或交互，未新增画布库或将原型重写为 React。
- 数据边界：项目、文件夹、新建、重命名、移动和删除仅使用 React 内存演示数据，刷新页面恢复默认值；演示数据集中在 `src/data/demo/ai-canvas-projects.js`。
- 验证结果：原型哈希回归测试 2/2 通过；全量 `src` ESLint 0 error，保留 5 条既有图片性能 warning；`next build --webpack` 通过并生成 28 个页面。浏览器验证新建项目、文件夹进入/返回、项目菜单、重命名与移动弹窗、全屏画布、原型添加与提示词节点交互、原型返回项目列表均可用；项目管理页在 1280px、1024px、390px 无整页横向溢出，页面控制台无错误。原画布沿用既有桌面布局，390px 下顶部工具区会裁切，本次按原样复用边界不调整。
- 关联文件：`src/app/ai-canvas/page.js`、`src/app/ai-canvas/CanvasProjectsClient.jsx`、`src/data/demo/ai-canvas-projects.js`、`public/prototypes/xianma-ai-canvas-v1.html`、`public/prototypes/assets/project-logo.png`、`tests/ai-canvas-prototype.test.mjs`、`PROJECT_MEMORY.md`。

### 2026-08-20：无限画布顶部按钮样式优化

- 状态：已按用户截图意见完成本地前端演示调整；未修改静态原型源文件或既有交互逻辑。
- 返回入口：原型内仅显示箭头的返回按钮在接入态隐藏，改由外层提供“左箭头 + 返回项目”深色按钮，保留清晰的悬停、焦点和返回项目列表交互，并与项目名称胶囊保持独立间距。
- 生成入口：原型右上角生成按钮在接入态运行时覆盖为山茶红 `#D9353F`，补齐悬停、按下、键盘焦点和禁用样式；其他画布控件保持原样。
- 验证结果：相关文件 ESLint 0 error；`next build --webpack` 通过并生成 28 个页面；原型哈希回归测试继续 2/2 通过。1280px 浏览器实测无横向溢出，返回按钮与生成按钮均完整位于视口内，返回交互可用，新的无缓存页面控制台无错误。
- 关联文件：`src/app/ai-canvas/CanvasProjectsClient.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-20：数据智能开发口径简化与创作链规则确认

- 状态：已确认保留当前报表全部指标和页面内容；本次只记录产品统计规则，不修改前端原型，不输出数据智能 PRD。
- 后续使用结果：取消生成后 7 天观察期，改为统计所选时间内生成且截至查询时至少发生过一次成功后续行为的去重结果；同一结果多个动作只计 1 个，后续发生使用时允许历史统计自然更新。
- 规则简化：一期不做少样本标记、阈值配置和条件触发；管理参考固定按功能使用、资产复用、积分效率三个维度输出。部分成功任务计入成功任务，只有实际成功结果进入成功结果与后续使用结果统计。
- 组织与团队：事件或任务创建时必须保存组织信息，“组织未知”仅作异常兜底；团队使用按资源所有人所属一级部门及其下级组织统计，其他一级部门使用计为跨团队使用。
- 创作链：无来源关系的首次提交创建新链；继续编辑、基于结果重新生成、带入其他工具和历史继续编辑沿用原链并增加业务轮次；系统失败自动重试不增加轮次；同一任务多张候选属于同一轮。首轮任一成功结果发生采纳行为，创作链计为首轮直接采纳 1 条。
- 趋势分桶：今日改为每 2 小时一个区间，共 12 个区间；2～31 天按日；32～180 天从开始日期起每 7 天；超过 180 天按月。积分使用趋势和人员洞察使用趋势共用该规则。
- 指标说明：面向管理者统一使用业务语言，分为“指标解读、统计口径、管理参考”三段。“指标解读”说明数据反映的组织状态和变化意义，“统计口径”说明计算、去重及包含排除范围，“管理参考”说明可辅助的决策及判断边界；页面不展示内部字段名、英文状态值或埋点参数名。
- 指标说明样式：取消三段内容的外层整框，只使用小标题、正文、间距和浅色分隔线区分层次。任务类指标直接表述为按任务去重并依据最终业务状态统计，其中部分成功计为成功。
- 原型同步：按小时分桶、7 天观察期说明和少样本标记已在本次后续修正中移除；真实数据能力、权限、验收、部署和上线均未据此完成。

### 2026-08-20：数据智能人员功能筛选缺陷修复

- 状态：已修复人员洞察切换功能筛选时因调用失效趋势缩放方法导致页面报错的问题；移除未使用的旧趋势计算，继续由统一时间分桶方法根据所选人员和功能汇总生成趋势。
- 验证结果：全部 14 个功能筛选均完成浏览器回归，13 个正常展示人员数据，1 个无人员记录的功能正确展示空状态；AI 提示词趋势实际使用次数合计与个人汇总一致，页面无新增错误。
- 工程检查：相关文件定向 ESLint 通过，`next build --webpack` 通过并生成 28 个页面，3000 端口本地原型已重启；本次没有修改指标、演示数据、布局或真实数据能力边界。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-20：数据智能管理者指标说明调整

- 状态：已将指标说明统一调整为“指标解读、统计口径、管理参考”；指标解读从管理者视角说明数据反映的组织状态和变化意义，统计口径保留计算与包含排除边界，管理参考说明组合判断方式、可辅助的决策及判断限制。
- 样式：取消三段内容的外层整框，只使用小标题、间距和浅色分隔线组织层次；页面不展示内部字段名、英文状态值或埋点参数名。
- 验证结果：普通任务及 AI 提示词专项共 28 个实际展示说明均完成浏览器检查；桌面和 390px 窄屏弹窗无溢出，关闭按钮及操作区可见，页面无新增错误。相关文件定向 ESLint 与 `next build --webpack` 通过并生成 28 个页面，3000 端口原型已重启。
- 范围边界：本次只调整前端原型说明文案与样式，不修改指标、计算、演示数据、趋势或真实数据能力边界。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`PROJECT_MEMORY.md`。

### 2026-08-20：数据智能领导提交前最终口径同步

- 状态：已将用户确认的数据智能统计规则同步到 `/admin/data` 前端演示原型并完成本地验证；本次不输出 PRD，不代表真实埋点、接口、组织权限、验收、部署或上线已经完成。
- 趋势分桶：今日统一按每 2 小时展示 12 个区间；2～31 天按日；32～180 天从开始日期起每 7 天；超过 180 天按月。积分使用趋势和人员洞察使用趋势共用同一规则。
- 一期简化：取消生成后 7 天观察期、少样本标记及固定阈值管理信号；后续使用结果统计所选时间内生成且截至查询时至少发生过一次成功后续行为的去重结果，历史统计可随之后发生的使用自然更新。
- 展示调整：资产与复用主题移除固定阈值的资产管理信号；管理关注项副标题统一为“从产出使用、创作过程、资产复用与积分效率观察平台价值”。指标说明继续使用“指标解读、统计口径、管理参考”三段，仅以小标题、间距和浅色分隔线区分，不为每段单独套卡片。
- 验证结果：相关文件定向 ESLint、`git diff --check` 和 `next build --webpack` 均通过，构建生成 28 个页面；浏览器确认两张今日趋势各有 12 个两小时区间，六个主题无“样本较少”和旧固定阈值文案，指标说明为管理者业务语言，页面控制台无错误。3000 端口已运行最新生产构建。
- 关联文件：`src/app/admin/data/DataOverviewClient.jsx`、`PROJECT_MEMORY.md`。

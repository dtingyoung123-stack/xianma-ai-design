# 产品需求池

本文件记录尚未进入开发确认阶段的产品需求。需求被记录不代表已经排期、实现或具备真实 API 能力。

## PRD-003 积分申请与审批

- 来源：平台用户与系统管理
- 状态：前端交互原型实施中，待后端用户、权限、积分账户与审计能力接入
- 确认时间：2026-08-01

### 需求目标

将原有钉钉积分申请迁入先马 AI 设计平台。用户积分不足时可在系统内提交申请，系统管理员审批通过后自动增加积分，并保留申请记录、审批记录和积分流水。

### 首期范围

- 用户从顶栏积分入口进入积分中心，查看余额、申请记录和积分流水。
- 姓名、部门和账号由当前登录用户信息自动带出，不允许手工代填。
- 用户填写申请事由和申请积分；默认 1000 积分，单次上限 5000 积分。
- 同一用户存在待审批申请时禁止重复提交。
- 系统管理员可查看、筛选、通过或驳回申请；驳回必须填写审批意见。
- 审批通过时同步更新申请状态、用户积分余额和积分流水，已处理申请不可重复审批。
- 首期仅系统管理员拥有审批权限；按部门、组织或业务线配置审批人的能力待组织权限完成后再优化。

### 产品与技术边界

- 当前项目仅实现前端完整交互原型，使用明确隔离的演示数据和浏览器本地状态。
- 真实上线必须由服务端校验登录用户和管理员权限，并使用事务完成审批与积分入账。
- 服务端需要保证审批幂等，记录审批人、审批时间、入账流水和关联申请单号。

### 验收标准

- 默认申请值为 1000，输入超过 5000、非整数或无效事由时无法提交并显示明确反馈。
- 存在待审批申请时申请按钮禁用，并提示待审批金额。
- 管理员审批通过后，用户余额增加对应积分，申请状态和流水同步更新。
- 驳回申请必须填写原因，用户可查看驳回意见并重新申请。
- 页面覆盖空状态、无搜索结果、错误反馈、禁用状态和窄屏布局。

## PRD-001 图层式局部编辑

- 来源：电商运营
- 状态：需求已记录，待产品方案确认
- 记录时间：2026-07-31

### 需求目标

运营希望像 Photoshop 图层一样，直接选中成品图片中的文字、商品主体或指定区域，对选中对象进行单独编辑，不需要整张图片重新生成。

### 用户场景

- 商品主图中的某一段文字需要替换，但商品和背景必须保持不变。
- 商品主体需要移动、替换、删除或调整颜色，但版式和其他元素必须保持不变。
- 局部区域需要进行材质、纹理、光影或细节优化，避免重新生成导致商品结构和页面信息漂移。

### 核心能力

1. 识别和选取图片中的文字、主体或手动指定区域。
2. 将选中对象以独立可操作图层呈现，并显示图层类型和可编辑状态。
3. 支持修改、替换、删除、移动和调整层级。
4. 支持颜色调整及局部细节优化。
5. 未选中区域保持不变，并支持编辑前后对比、撤销和恢复。

### 与现有功能的关系

- 现有“一键改字”继续作为文字修改的快捷入口。
- 图层式局部编辑是更通用的高级编辑能力，覆盖文字、商品主体和任意区域。
- 后续方案应优先复用现有区域遮罩编辑器、图片预览和工作台框架，不重复建设同类交互。

### 产品与技术边界

- 普通 PNG/JPG 不包含真实图层，需要通过 OCR、主体分割、区域分割、图像修复和生成式编辑等能力重建可编辑对象。
- 前端可以先完成图层面板、选区、变换和状态管理，但自动识别、替换、删除及细节优化依赖真实模型 API。
- 不得把静态演示或伪造结果标记为已上线能力。
- 商品文字、商标、结构和图案属于电商高风险信息，编辑结果必须支持人工确认后导出。

### 待确认问题

- 第一阶段优先支持文字、商品主体还是手动区域。
- 图层识别是自动完成，还是由用户点击后按需识别。
- 是否需要图层顺序、锁定、隐藏、复制和合并能力。
- 输出是否必须保持原图尺寸、透明背景和可继续编辑的数据格式。
- 模型失败、识别不完整及编辑结果不符合预期时的回退流程。

### 初步验收方向

- 用户能够明确选中目标对象并看到独立图层状态。
- 对单个对象的编辑不会明显改变未选中区域。
- 所有模型操作均有处理中、失败、重试和人工确认状态。
- 编辑历史可撤销，原图始终可以恢复。

## PRD-002 全局吸色与提示词颜色约束

- 来源：电商运营
- 状态：买家秀前端原型验证中，待确认后推广
- 记录时间：2026-07-31

### 需求目标

商品实拍或生成结果容易出现偏色。用户需要从当前页面中的参考图片直接吸取目标颜色，复制准确色值后按当前页面的实际用途粘贴使用。

### 用户流程

1. 打开公共图片预览弹窗，在工具栏点击统一的吸色图标。
2. 使用系统取色器点击预览大图中的任意图片像素。
3. 工具栏显示颜色色块与 HEX，并自动复制 HEX，支持直接粘贴和再次复制。
4. 页面颜色记录只显示色块和 HEX，不自动修改提示词，也不预设主体色、参考色等业务含义。
5. 用户可再次复制或删除已吸取的颜色记录。

### 当前范围

- 公共图片预览弹窗统一接入 AI 买家秀、AI 商品套图和一键改字。
- 各页面默认只提供吸色、自动复制和再次复制能力，不自动写入提示词或其他输入框。
- 单个任务最多保存 4 个不同颜色，不为颜色增加页面相关的用途标签。
- 浏览器不支持系统取色器时，回退到原生颜色选择控件。
- 后续使用 `ImageQueueModule` 的工作台页面默认复用同一预览组件，不单独实现预览和吸色交互。

### 验收标准

- 每个页面只在公共图片预览弹窗显示一个吸色入口，图片缩略图和图片队列标题不重复放置吸管按钮。
- 取色后色块和 HEX 同步出现，重复色值不会重复添加。
- 点击颜色标签的复制按钮后，HEX 写入剪贴板并显示成功反馈。
- 吸色和删除颜色均不得改变用户手写提示词。
- 达到 4 个颜色时阻止继续添加并给出明确反馈。

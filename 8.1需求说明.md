# AI 设计平台功能优化 PRD

## 一、需求背景

提升图片局部编辑、文字修改和颜色获取效率，减少用户反复上传、整体重生成及手动查找色值的操作成本。同时将可复用能力沉淀为全局公共组件，保证不同业务页面交互一致。

## 功能 1：AI 买家秀支持区域编辑

### 需求目标

用户上传商品图片后，可以标记需要 AI 重点识别或修改的图片区域。

### 功能要求

- 商品图片操作区增加“区域编辑”入口。
- 点击后打开区域编辑弹窗。
- 支持画笔涂抹选区。
- 支持橡皮擦除选区。
- 支持调整画笔大小。
- 支持撤销、重做、清空。
- 支持显示或隐藏遮罩。
- 支持缩放、拖动查看图片。
- 保存后在图片列表显示“已设置区域”状态。
- 再次打开时保留上一次编辑内容。
- 删除或替换图片时，对应区域数据同步处理。

### 验收标准

- 用户可以完成选区绘制、修改和保存。
- 遮罩位置与原图保持准确对应。
- 关闭未保存时不覆盖原有数据。
- 保存后可再次进入并继续编辑。

## 功能 2：区域编辑新增自定义文本标注（全局统一）

### 需求目标

用户可以在图片指定位置添加文字说明，帮助 AI 理解区域含义或编辑要求。

### 功能要求

- 在公共区域编辑器中增加“文本标注”工具。
- 点击图片位置后创建文本标注。
- 标注内容由用户自由输入，不自动编号。
- 支持多个文本标注。
- 按住标注圆点或文字框均可拖动位置。
- 单击文字框进入内容修改。
- 拖动和单击修改需要明确区分，避免拖动时触发编辑。
- 支持删除单条标注。
- 标注需要保持醒目，并与图片内容形成足够对比。
- 标注位置按照图片坐标保存，缩放后不发生偏移。
- 该能力沉淀为公共区域编辑组件，供其他页面复用。

### 验收标准

- 可以新增、修改、移动和删除文本标注。
- 圆点与文字框移动关系正确。
- 拖动过程中不会误触发文字编辑。
- 图片缩放或弹窗尺寸变化后，标注位置保持准确。

## 功能 3：一键改字页面交互优化

### 需求目标

识别图片中的全部文字，让用户直接选择需要修改的文字，不再手动填写“原文字”。

### 功能要求

- 支持本地上传和素材库选择图片。
- 上传后支持识别图片中的全部文字。
- 识别结果以文字列表和图片选框两种形式展示。
- 点击文字列表时同步选中图片中的对应区域。
- 点击图片文字区域时同步定位文字列表。
- 用户可直接填写替换后的文字内容。
- 支持单条启用、取消和删除。
- 支持手动框选补充未识别到的文字区域。
- 文字框支持拖动、调整位置和尺寸。
- 支持撤销、重做和重置。
- 保留提示词、模型选择、清晰度、画质、比例和生成数量设置。
- 页面右上角提供统一历史记录入口。
- 移除独立的“原文字/目标文字”输入流程。

### 状态要求

- 未上传图片：禁用文字识别和提交。
- 识别中：显示明确的处理中状态。
- 未识别到文字：允许用户手动框选。
- 存在未填写的替换内容：提交前给予提示。
- 清空页面：同步清空图片、文字列表和编辑记录。

### 验收标准

- 能识别并展示图片中的全部文字区域。
- 列表和图片选区双向联动。
- 用户可以逐项修改识别文字。
- 手动补充、移动和删除文字区域可正常使用。
- 模型、参数、历史记录等公共组件交互一致。

## 功能 4：图片预览新增吸色功能（全局统一）

### 需求目标

用户在查看大图时可以直接吸取图片颜色，并复制 HEX 色值用于提示词或其他业务输入。

### 功能要求

- 所有工作台页面统一使用公共图片预览弹窗。
- 预览弹窗保留缩放、拖动、翻转、切换和下载功能。
- 工具栏增加吸管图标，放在下载按钮左侧。
- 点击吸管后，可以吸取当前屏幕中预览图片的颜色。
- 取色成功后自动复制 HEX 到剪贴板。
- 用户可以直接粘贴色号。
- 工具栏显示色块、HEX 和再次复制按钮。
- 页面颜色记录只显示色块和 HEX。
- 不显示“产品主体”“参考色”等业务标签。
- 支持删除已保存的颜色记录。
- 不自动新增、修改或删除提示词内容。
- 浏览器不支持系统吸管时，回退到原生颜色选择器。
- 该能力作为公共图片预览组件，供所有业务页面统一复用。

### 验收标准

- 打开图片预览后可以正常启动吸管。
- 吸色过程中预览图片保持可见。
- 获取的 HEX 可以直接粘贴。
- 再次复制和删除操作正常。
- 吸色不会改变用户提示词。
- 各页面预览弹窗的样式和操作保持一致。
